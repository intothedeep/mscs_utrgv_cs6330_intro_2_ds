# 강의 덱 → 장 설계 (Deck-to-Chapter Architecture)

작성: system-architect, 2026-09-23, cycle 1/2. 설계 문서다. LaTeX 본문과 PLAN.md 과제는
여기서 만들지 않는다(각각 sonnet-writer, product-manager 몫).

출처는 `book_intro_2_ds/notes_text/<deck>/slides.md` 와 `media/` 뿐이다. 슬라이드 번호는
`slides.md` 의 `## Slide N` 을 따른다.

## 0. 전제와 과제 지시문 정정

1. **덱 9에는 지수분포(Exponential)나 기하분포(Geometric) 계산이 없다.** 덱 9의 계산은
   슬라이드 13의 $0.4^3 = 0.064$ 하나와 시뮬레이션 표 두 개(슬라이드 11, 12)뿐이다.
   지시문이 말한 지수/기하 연쇄 계산($P(0<x<1)=0.05$, $0.95\cdot0.05$, $0.95^2\cdot0.05$)은
   **덱 6 슬라이드 16~18** 에 있다(`6_DataDistribution/media/image26~29.png`).
   결정: 이 계산은 `04-distributions.tex` 의 `sec:exponential` 에 둔다(4.3절 참조).
   가설검정 장은 "독립 시행의 확률은 곱한다"는 규칙만 공유하므로 `\Cref{sec:binomial}` 로
   연결만 한다.
2. **기존 장 01~04는 이미 덱 3~6과 1:1로 대응한다.** 01↔덱 3(일부), 02↔덱 4, 03↔덱 5,
   04↔덱 6. 따라서 기존 파일 번호는 강의 번호가 아니다. 이 사실이 2절 이름 규칙 결정의 출발점이다.
3. **기존 장 본문은 줄표(LaTeX 하이픈 세 개)를 많이 쓴다.** book.md 원칙 11 이전에 쓴 글이다. 새 장과
   채우기(fill) 작업은 원칙 11을 따른다. 기존 줄표 정리는 요청이 없으므로 하지 않는다
   (latex.md §13 최소 diff). 6절 Q6에서 소유자에게 묻는다.
4. **기존 장은 원칙 12의 여섯 단계 뼈대(In brief · 배경 · 왜 · 무엇 · 어떻게 적용 · 앞으로)를
   갖추지 않았다.** 채우기 작업 때 뼈대를 덧붙일지는 6절 Q2.

## 1. 커버리지 표 (Coverage Table)

| 덱 | 슬라이드 수 | 기존 장 | 상태 | 조치 | 근거 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 1_Syllabus | 12 | 없음 | none | **skip** | 수업 운영 정보(시간, 성적 비율, AI 정책, 표절)뿐이다. 개념이 없으니 원칙 12의 "왜 · 무엇"을 채울 대상이 없다. 슬라이드 3~4의 주제 목록(가설검정, 통계적 유의성, 순열검정(Permutation Test) 등)은 각 장의 "앞으로" 절에서 출처로만 인용한다. |
| 2_Data | 25 | 없음 | none | **skip (장 없음), 슬라이드 9~11만 수확** | 슬라이드 대부분이 동기 부여, 역사, 뉴스 사례(빅데이터 규모, 선거, Target)다. 다시 계산할 수식이 없다. 단 슬라이드 9~11(깨끗한 데이터, 데이터 문제 목록)은 실질 내용이므로 lec07의 결측값 절과 02의 `sec:missing` 에서 출처로 인용한다. 원칙 12("노트 하나 = 장 하나")에서 벗어나므로 소유자 확인 필요(Q3). |
| 3_DataAndMatrix | 20 | `01-data-attributes` | partial | **fill 01** | 슬라이드 2~4(척도 네 가지)는 01이 다룬다. 슬라이드 2의 이산/연속(Discrete/Continuous), 5~19(데이터 행렬, 차원, 벡터, 모든 입력을 행렬로)는 없다. |
| 4_BasicStats | 23 | `02-descriptive-statistics` | partial | **fill 02** | 평균, 중앙값, 저항성, 분산/표준편차는 02가 다룬다. 슬라이드 4~11(확률변수(Random Variable), PMF, PDF, CDF, 주사위 예제, $P(X>3)=0.6$ 예제), 19(최빈값의 저항성), 20(범위(Range))은 없다. |
| 5_StatandVisualization | 28 | `03-visualization` | covered | **skeleton retrofit만** (Q2) | 슬라이드 2~27이 전부 03에 있다(분위수 예제 2.325, 히스토그램, 에러바, 바이올린, 바코드, 박스플롯, 노치, CI, 부트스트랩). |
| 6_DataDistribution | 30 | `04-distributions` (58줄 stub) | partial | **fill 04** | 정규분포 정의식만 있다. 지수, 균등, 이항 절은 TODO다. 멱법칙(Power Law, 슬라이드 19~22)은 절 자체가 없다(그림 `06_06_powerlaw_vs_exponential.png` 는 이미 존재). |
| 7_qqPlotandNormalization | 26 | 없음 | none | **new chapter `lec07`** | Q-Q 플롯, 이산화, 정규화, 로그 변환, 결측값 처리 전부 미수록. |
| 9_Hypothesis_Testing | 14 | 없음 | none | **new chapter `lec09` (파일럿)** | 03의 `sec:ci` 가 "5% 미만이면 유의"를 한 줄 언급할 뿐, 귀무가설/대립가설/기각 논리는 없다. |

덱 8은 없다. 나중에 오면 `lec08` 로 끼워 넣는다(2절).

## 2. 파일 이름과 순서 (File Naming and Ordering)

### 2.1 규칙 충돌

- book.md §3: 장은 `chapters/NN-slug.tex`, 번호는 "파일 정렬 = 읽는 순서"를 위해 붙인다.
- book.md 원칙 12: 강의 노트 장은 `chapters/lecNN-slug.tex`.
- latex.md §13: 요청 없이 파일 이름을 바꾸지 않는다.

같은 book.md 안의 두 문장이 서로 다른 이름을 지시한다. core.md에 따라 로컬에서 해결하지 않고
소유자에게 올린다(Q1). 아래는 이름을 바꾸지 않는 기본안이다.

### 2.2 사실: 이름 변경 없이는 "전체 파일 정렬 = 읽는 순서"가 불가능하다

새 개념 장(덱 7, 9)은 1부(기초)에 들어가야 한다. 그런데 `05-`, `06-` 은 이미 2부(실전)의
과제/프로젝트 장이 차지했다. `07-`, `08-` 로 붙이면 정렬은 01~04, 05~06(2부), 07~08(1부)이
되어 읽는 순서와 어긋난다. `04a-`, `04b-` 같은 끼워 넣기는 덱이 더 올 때마다 문자가 늘어나는
임시방편이라 쓰지 않는다.

### 2.3 권고 (기본안, 이름 변경 없음)

1. **새 강의 장은 `chapters/lecNN-slug.tex`, NN = 덱 번호(두 자리).**
   예: `lec07-qq-normalization.tex`, `lec09-hypothesis-testing.tex`.
   - 이유: 덱 번호는 바뀌지 않는다. 덱 8이 나중에 와도 `lec08` 이 자동으로 07과 09 사이에
     정렬된다. 순번(NN)을 쓰면 중간 덱이 올 때마다 뒤 파일을 전부 다시 번호 매겨야 한다.
   - 원칙 12의 이름과 일치한다.
2. **기존 파일(`00`~`06`, `99`)은 그대로 둔다.**
3. **읽는 순서의 유일한 권위는 `parts/*.tex` 의 `\input` 순서다.** 파일 정렬이 아니다.
   `lec*` 끼리는 파일 정렬 = 강의 순서 = 읽는 순서가 성립한다.
4. **배치:** 새 강의 장은 전부 `parts/part1-foundations.tex`(기초)에, 04 뒤에 덱 번호 순으로 둔다.

   ```
   % part1-foundations.tex
   \input{chapters/01-data-attributes}      % 덱 3
   \input{chapters/02-descriptive-statistics} % 덱 4
   \input{chapters/03-visualization}        % 덱 5
   \input{chapters/04-distributions}        % 덱 6
   \input{chapters/lec07-qq-normalization}  % 덱 7
   \input{chapters/lec09-hypothesis-testing} % 덱 9
   ```
   2부(실전)는 손대지 않는다. PDF의 장 번호는 `\input` 순서로 매겨지므로 가설검정 장은
   "6장"이 되고 과제 1 장은 "7장"으로 밀린다. 본문이 전부 `\Cref` 를 쓰므로 깨지는 참조는 없다
   (손으로 박은 장 번호가 없음을 grep으로 확인할 것: pm의 수용 기준 후보).
5. **라벨:** 장 라벨은 주제 이름으로 짓는다(`ch:hypothesis`, `ch:qq-normalization`).
   파일 이름과 무관하므로 Q1의 결론이 무엇이든 라벨은 바뀌지 않는다.

### 2.4 소유자에게 올릴 선택지 (Q1)

| 안 | 내용 | 비용 | 결과 |
| :-- | :-- | :-- | :-- |
| (a) 기본안 | 새 장만 `lecNN`, 기존 유지 | 0 | 두 이름 체계 공존. 순서 권위는 `parts/` |
| (b) 01~04를 `lec03`~`lec06` 으로 개명 | `git mv` 4회 + `\input` 4줄 | 작음 | 1부 전체가 한 체계. 2부와 부록은 여전히 `NN` |
| (c) 2부를 `50-`, `60-` 대역으로 개명하고 새 장은 `05-`, `07-`... | `git mv` 2회 + `\input` 2줄 | 작음 | `NN` 한 체계. 단 덱 8이 늦게 오면 끼울 번호가 없어질 수 있다 |

권고는 (a)로 시작하고, 소유자가 원하면 (b). 어느 안이든 book.md §3과 원칙 12 중 하나를 고쳐
두 문장이 같은 말을 하게 만드는 것은 소유자 몫이다.

## 3. 파일럿: 덱 9 가설검정 (Hypothesis Testing) 장 개요

- 파일: `chapters/lec09-hypothesis-testing.tex`, 라벨 `ch:hypothesis`
- 장 제목(줄표 없음): `가설검정: 증거가 귀무가설을 터무니없게 만드는가`
  (슬라이드 2, 8의 "make H0 look ridiculous"를 그대로 살린 제목)
- 출처: `notes/9_Hypothesis Testing.pptx` (14 slides). 슬라이드에 외부 문헌 표기가 없으므로
  bib 항목을 만들지 않는다. 슬라이드 자체를 출처로 적는다.
- 쓸 환경(preamble에 이미 있음): `inbrief`, `keyidea`, `pitfall`, `aside`, `definition`, `example`.
  새 매크로는 필요 없다. 필요하면 `\DeclareMathOperator` 대신 기존 `\E`, `\Var` 를 쓴다.

### 3.1 장 머리 In brief (원칙 12의 1단계)

- **Why:** 표본 하나에서 본 차이가 진짜인지 우연인지 가르는 규칙이 필요하다.
- **When:** "차이가 없다(또는 기존 값 그대로다)"를 기본값으로 두고, 데이터가 그 기본값을 뒤집을 만큼 강한지 물을 때.
- **Where:** 두 약의 회복 시간 비교, 비율(수컷 강아지 비율, 반항적 인간 비율), 성공 확률(복권) 같은 모수(Parameter)에 대한 주장.
- **How:** 귀무가설(Null Hypothesis) $H_0$ 와 대립가설(Alternative Hypothesis) $H_a$ 를 세운다 → 기준 $\alpha$ 를 미리 정한다 → $H_0$ 가 참일 때 관측값 이상으로 극단적인 결과가 나올 확률을 구한다 → 기준보다 작으면 기각(Reject), 아니면 기각 실패(Fail to Reject).

### 3.2 절 구성 (여섯 단계 뼈대 + 무엇 안의 세부 절)

각 개념 절은 원칙 7의 `inbrief` 네 줄로 연다. 아래 한 줄씩이 그 초안이다.

#### (1) 배경 `sec:ht-background` (개념 절 아님, In brief 생략)

선공급할 선행 개념(원칙 9a). 이미 다룬 것은 한 줄 요약 + `\Cref`, 처음인 것은 그 자리에서 두 문장.

| 선행 개념 | 어디서 다뤘나 | 이 장에서의 처리 |
| :-- | :-- | :-- |
| 평균, 퍼짐(표준편차) | `\Cref{sec:center}`, `\Cref{sec:spread}` | 한 줄 요약 + 링크. 3.2 (8)절의 "평균에서의 거리"에 쓴다 |
| 표본(Sample)과 모집단(Population) | `\Cref{sec:skew-compute}` 에서 스치듯 | 두 문장으로 직접 정의. "우리가 보는 것은 표본, 주장하는 것은 모집단" |
| 모수(Parameter) | 없음 | 두 문장으로 정의. 슬라이드 10의 "parameter = value" 를 읽으려면 필요 |
| 확률, 독립 시행의 곱셈 규칙 | 없음 (`sec:binomial` 은 stub) | 두 문장 + 동전 두 번 예. 슬라이드 13의 $0.4^3$ 에 필요 |
| 이항분포(Binomial Distribution) | `\Cref{sec:binomial}` (현재 stub, 4.3절에서 채움) | 한 문단 요약. "$n$ 번 시행 중 성공 횟수". 링크는 stub이어도 컴파일된다 |
| 신뢰구간과 "5% 미만이면 유의" | `\Cref{sec:ci}`, `\Cref{subsec:ci-compare}` | 한 줄 요약 + 링크. 덱 5가 이미 5% 기준과 "p-value < 0.05" 라는 말을 썼다(덱 5 슬라이드 24, 26, 27) |
| 부트스트랩(재표집) | `\Cref{subsec:bootstrap}` | 한 줄. "컴퓨터로 반복해서 분포를 만든다"는 발상이 슬라이드 11~12의 시뮬레이션과 같다 |

용어 주의: 덱 9는 "p-value", "significance level" 이라는 말을 쓰지 않는다("significance test"만
쓴다). 이 장은 덱 9가 계산하는 양(슬라이드 11, 12의 "13 이상일 확률")에 p값(p-value)이라는
이름을 붙이고, 그 이름이 덱 5에서 이미 나왔음을 밝힌다.

기호 표 `tab:ht-symbols` (원칙 9c):

| 기호 | 뜻 | 읽기 |
| :-- | :-- | :-- |
| $H_0$ | 귀무가설(Null Hypothesis): 차이 없음, 기존 값 그대로 | 에이치 제로 |
| $H_a$ | 대립가설(Alternative Hypothesis): 찾으려는 주장 | 에이치 에이 |
| $p$ | **두 가지 뜻이 있다.** (i) 모수로서 성공 확률(슬라이드 11: $p = 50\%$), (ii) p값 | 반드시 경고 상자로 구분 |
| $\alpha$ | 유의수준(Significance Level): 미리 정한 기각 기준(덱 9는 1%, 덱 5는 5%) | 알파 |
| $n$, $X$ | 시행 횟수, 성공 횟수 | |

`pitfall` 하나: 기호 $p$ 가 슬라이드 11에서 "수컷일 확률"(모수)로 쓰이고, 이 장에서는 p값으로도
쓰인다. 모수는 $p$ 대신 $\pi$ 나 $\theta$ 로 바꾸지 말고(슬라이드와 어긋남), 본문에서 p값은 항상
"p값"이라는 한글 단어로 쓰고 모수만 기호 $p$ 로 쓴다.

#### (2) 왜 필요한가 `sec:ht-why` (원칙 8의 문제 → 처방)

- 문제 1(슬라이드 3): 같은 약을 먹어도 회복 시간이 사람마다 다르다. 실험 한 번의 평균 차이
  (A가 B보다 15시간 빠름)는 다시 하면 달라진다.
- 문제 2(슬라이드 4): 다시 해 보니 오히려 A가 35시간 더 느렸다. 한 번의 결과로 세운 가설이
  뒤집힌다. 이 경우는 확신을 갖고 기각한다.
- 문제 3(슬라이드 5): 13시간 가설은 "첫 실험이 13이었다"는 이유 하나로 정해졌다. 재실험(12시간,
  13.5시간)은 기각할 만큼 다르지도, 맞다고 확신시킬 만큼 같지도 않다.
- 처방(슬라이드 8): 예비 자료로 숫자를 정하는 대신, 예비 자료가 필요 없는 유일한 값 **0**(차이 없음)을
  가설로 삼는다. 이것이 귀무가설이다.
- 그림: `fig:ht-drug-experiments` (아래 3.4).

#### (3) 무엇인가: 세부 절

| 절 | 라벨 | In brief 초안 (Why / When / Where / How) | 슬라이드 |
| :-- | :-- | :-- | :-- |
| 귀무가설과 대립가설 | `sec:ht-hypotheses` | Why: 증거를 잴 기준점(차이 0)이 필요하다. / When: 데이터를 보기 **전에** 세운다. / Where: 두 집단 비교, 비율, 확률에 대한 주장. / How: $H_0$ 는 등호("차이 없음", "parameter = value"), $H_a$ 는 찾으려는 주장. | 2, 6, 8, 9 |
| 기각과 기각 실패 | `sec:ht-decision` | Why: 결론은 두 가지뿐이고, "채택"은 없다. / When: 증거 확률을 기준과 비교한 직후. / Where: 모든 검정의 마지막 문장. / How: 증거가 $H_0$ 를 터무니없게 만들면 기각, 아니면 "기각 실패"라고만 쓴다. | 2, 4, 5, 7 |
| 대립가설의 세 형태 | `sec:ht-forms` | Why: 무엇을 "극단적"이라 부를지는 $H_a$ 의 방향이 정한다. / When: $H_0$ 를 세울 때 함께, 데이터를 보기 전에. / Where: "더 크다"(슬라이드 11~13), "다르다"(슬라이드 9). / How: $>$, $<$ 는 한쪽 꼬리(단측검정, One-sided Test), $\neq$ 는 양쪽 꼬리(양측검정, Two-sided Test). | 9, 10 |
| p값과 유의수준 | `sec:ht-pvalue` | Why: "터무니없다"를 숫자 하나로 바꿔야 판정이 재현된다. / When: 가설과 방향을 정한 뒤. / Where: 슬라이드 11~13이 모두 이 양을 계산한다. / How: $H_0$ 가 참이라 가정하고 "관측값 이상으로 극단적인 결과"의 확률을 구해 $\alpha$ 와 비교. | 11, 12, 13 (+ 덱 5 슬라이드 24, 26) |
| 시뮬레이션으로 p값 구하기 | `sec:ht-simulation` | Why: 공식을 몰라도 "$H_0$ 세상"을 컴퓨터로 여러 번 만들어 보면 된다. / When: 분포 공식이 없거나 복잡할 때. / Where: 강아지 성비, 반항적 인간 비율. / How: $H_0$ 로 1000회 모의 → 관측값 이상인 횟수를 센다 → 1000으로 나눈다. | 11, 12 |
| 정확한 계산 | `sec:ht-exact` | Why: 경우가 단순하면 시뮬레이션 없이 곱셈만으로 정확한 값이 나온다. / When: 독립 시행이고 확률이 알려져 있을 때. / Where: 복권 3연승, 강아지 문제의 이항 계산. / How: 독립이면 곱한다. 여러 경우면 이항계수로 센다. | 13, (11) |
| 두 집단 비교: 평균에서의 거리 | `sec:ht-two-groups` | Why: "차이가 있다"는 두 집단 평균을 따로 둘 때 점들이 평균에 훨씬 가까워진다는 뜻이다. / When: 두 집단의 연속값(회복 시간)을 비교할 때. / Where: 약 C 대 D. / How: 합친 평균 하나(귀무)와 집단별 평균 둘(대립)에서 거리를 재어 비교한다. 줄어든 양이 작으면 기각 실패. | 9 |

`sec:ht-decision` 안의 `pitfall`: "기각 실패는 $H_0$ 가 참이라는 증명이 아니다." 슬라이드 2(금성에서
외계 생명을 못 찾았다고 없다는 뜻은 아니다)와 슬라이드 5("don't convince us the hypothesis is
correct")가 근거. `\Cref{subsec:ci-compare}` 의 함정 "겹친다고 같은 것은 아니다"와 같은 논리임을
명시한다. 표 `tab:ht-wording`(올바른 표현 / 틀린 표현)을 둔다.

`sec:ht-pvalue` 안의 `pitfall`: 기준값 1%(덱 9)와 5%(덱 5, `\Cref{sec:ci}` 의 $1.96$)가 다르다.
기준은 법칙이 아니라 **데이터를 보기 전에 정하는 약속**이다. `\Cref{subsec:fence}` 의 "1.5는
관례다"와 같은 구조의 함정으로 연결한다.

`sec:ht-forms` 안의 `pitfall`: 슬라이드 12의 방향 문제(3.5의 E4 참조). 방향은 데이터를 보고 고르지
않는다.

#### (4) 어떻게 적용하는가 `sec:ht-apply` (장 끝)

- 다섯 단계 절차 상자(= `fig:ht-flow`): 가설 → $\alpha$ → 극단의 정의(방향) → p값(시뮬레이션 또는
  정확 계산) → 문장으로 결론.
- 이 책 안의 적용 자리:
  - `\Cref{subsec:ci-compare}`: 버거(평균 620, CI $[557, 686]$) 대 치킨너겟(평균 275, CI $[211, 345]$).
    같은 질문을 $H_0$: "두 평균 칼로리 차이 = 0" 으로 다시 쓰면 CI 비교가 곧 그림으로 한 가설검정임을 보인다.
    새 수치 계산은 하지 않는다(값은 03에 이미 있다).
  - `\Cref{subsec:bootstrap}`: 재표집으로 분포를 만든다는 발상이 `sec:ht-simulation` 과 같다.
    차이점 한 줄: 부트스트랩은 **표본**에서 뽑고, 가설검정 시뮬레이션은 **$H_0$ 가 정한 세상**에서 뽑는다.
  - `\Cref{ch:hw1}`: 과제 데이터에서 세울 수 있는 $H_0$ 예시 한두 개(계산 없이 가설 문장만).
  - `\Cref{ch:project}`: 프로젝트에서 "차이가 있다"고 쓰기 전에 거칠 점검 목록.
- 확인 문제 3개(슬라이드 14 Kahoot 자리를 대신): 기각/기각 실패 문장 고르기, 방향 고르기, $0.4^n$ 최소 $n$.

#### (5) 앞으로 `sec:ht-future`

- 순열검정(Permutation Test), 통계적 유의성(Statistical Significance), 차등 분석(Differential Analysis):
  강의 계획서(덱 1 슬라이드 3)의 "Statistical Inference" 목록에 있다. 다음 강의로 이어지는 갈래로만 적는다.
- 슬라이드 9의 "평균에서의 거리 비교"를 정식 검정 통계량으로 만드는 방법은 이 덱에 없다. 열린 질문으로 남긴다.
- 복권 예제가 드러낸 질문: $H_0$ 가 틀렸을 때 기각할 수 있는가(표본 크기의 문제). 덱에 없으므로 질문으로만 적는다.
- 외부 문헌은 붙이지 않는다.

### 3.3 슬라이드 → 절 대응 (모든 슬라이드)

| 슬라이드 | 내용 | 쓰는 절 | 비고 |
| :-- | :-- | :-- | :-- |
| 1 | 제목 | (장 제목) | 내용 없음 |
| 2 | 금성 외계 생명 $H_0$/$H_a$, "무엇을 배웠나: 아무것도" | `sec:ht-hypotheses`, `sec:ht-decision` | `image1.png` 은 영화 장면. 재현하지 않는다 |
| 3 | 약 A/B, 15시간 차이 | `sec:ht-why` | `image11.png`: A 평균 약 15, B 약 30. 축 눈금으로 15시간 확인됨 |
| 4 | 재실험에서 35시간 차이, 기각 | `sec:ht-why`, `sec:ht-decision` | 축 눈금 없음(`image12~15`). 35는 재계산 불가, 슬라이드 값으로만 인용 |
| 5 | 약 C/D 13시간 가설, 재실험 12, 13.5시간, 기각 실패 | `sec:ht-why`, `sec:ht-decision` | `image17`: C 약 17~18, D 약 30.5. 13 안팎 확인. 12, 13.5는 눈금 없음 |
| 6 | 귀무가설, 대립가설, 유의성 검정(Significance Test) 정의 | `sec:ht-hypotheses` | 정의 상자 `def:ht-null`, `def:ht-alt` |
| 7 | 약 E/F 0.25, 0.5시간 차이, 기각 실패 | `sec:ht-decision` | 두 재실험에서 **방향이 뒤집힌다**(F가 높다가 E가 높다). 그림의 눈대중(약 1시간)과 라벨(0.25)이 맞지 않으므로 라벨 값만 인용, 그림은 개략도로 취급 |
| 8 | 귀무가설은 예비 자료가 필요 없다(0) | `sec:ht-why`(처방), `sec:ht-hypotheses` | |
| 9 | 대립가설은 대개 귀무의 반대, 평균에서의 거리 | `sec:ht-hypotheses`, `sec:ht-two-groups` | `image22`(합친 평균 1개), `image23`(집단 평균 2개)이 TikZ 그림의 원형 |
| 10 | $H_0$: parameter = value, $H_a$ 세 형태 | `sec:ht-forms` | `image24~26` |
| 11 | 강아지 15마리 중 수컷 13 | `sec:ht-simulation`, `sec:ht-exact` | 예제 E1 |
| 12 | 400명 중 14% 반항적 | `sec:ht-simulation` | 예제 E4(연습용), 모호성 있음 |
| 13 | 복권 3번 중 3번 | `sec:ht-exact` | 예제 E2 |
| 14 | Kahoot | `sec:ht-apply` 확인 문제 | 내용 없음 |

### 3.4 다시 계산할 숫자 (전사 금지, 재계산 값)

| # | 슬라이드 | 슬라이드 값 | 재계산 | 판정 |
| :-- | :-- | :-- | :-- | :-- |
| R1 | 11 표 | 빈도 합 | $0+0+3+14+42+92+153+196+196+153+92+42+14+3+0+0 = 1000$ | 맞음 |
| R2 | 11 | $P(X \ge 13) = 3/1000 = 0.003$ | 표에서 $3+0+0 = 3$ → $0.003$ | 맞음 |
| R3 | 11 (정확값) | 없음 | $\binom{15}{13}+\binom{15}{14}+\binom{15}{15} = 105+15+1 = 121$, $121/2^{15} = 121/32768 = 0.00369$ (0.37%) | 결론(기각, $< 1\%$) 동일 |
| R4 | 11 표 | "1000회 시뮬레이션" | 각 행이 $1000\binom{15}{k}/32768$ 의 반올림과 **정확히 일치**한다(예: $k=7$: $196.4 \to 196$, $k=6$: $152.7 \to 153$, $k=13$: $3.2 \to 3$, $k=14$: $0.46 \to 0$). 완벽한 좌우 대칭도 그 증거 | **플래그:** 실제 무작위 시뮬레이션이 아니라 기댓값 표다. 본문에서 "기댓값 표이며 실제 시뮬레이션은 이 근처에서 흔들린다"고 밝힌다 |
| R5 | 12 표 | 빈도 합 | $7+40+93+173+327+253+73+27+7 = 1000$. 평균 $\sum k f_k / 1000 = 10.001\%$ | 맞음 |
| R6 | 12 | $P(\ge 14\%) = 7/1000 = 0.007$ | 표의 14 행만 읽으면 $7/1000$. 이론값: $X \sim \mathrm{Bin}(400, 0.1)$, 평균 $40$, 표준편차 $\sqrt{400 \cdot 0.1 \cdot 0.9} = 6$. 14% = $56$명. 연속성 보정 $z = (55.5-40)/6 = 2.58$ → 정규근사 약 $0.005$, 왜도 보정 시 약 $0.0067$ | 대체로 맞음. 정확값은 `scipy.stats.binom.sf(55, 400, 0.1)` 한 줄로 확인할 것(학습 아님, 초 단위 계산) |
| R7 | 12 표 | 표의 %가 정수 | 표본 400명의 비율은 0.25% 단위다. 표는 정수 %로 **묶은** 값이다. "14 행"이 $[13.5\%, 14.5\%)$ 라면 "14% 이상"은 $X \ge 54$, $z = (53.5-40)/6 = 2.25$ → 약 $0.012 \sim 0.015$ 로 **1% 기준을 넘는다** | **플래그(모호):** 구간 경계에 따라 판정이 뒤집힐 수 있다. 본문은 꼬리를 퍼센트가 아니라 **인원수**($X \ge 56$)로 정의하라고 쓴다. 이 예제는 주 예제로 쓰지 않고 연습/함정으로 쓴다 |
| R8 | 12 | $H_a$: 비율 $> 10\%$ | 외계인의 결정 규칙은 "10% **미만**이면 침공"이다. 결정에 맞는 질문은 $H_a$: $< 10\%$ 인데, 슬라이드는 관측값 14%를 본 뒤 $>$ 방향을 택한 것으로 읽힌다 | **플래그(방향):** 계산은 맞다. 방향 선택을 데이터 뒤에 하면 안 된다는 함정 재료로 쓴다 |
| R9 | 13 | $0.4 \times 0.4 \times 0.4 = 0.064$ | $0.4^3 = 0.064$. $H_a$ 가 "더 크다"이므로 p값은 $P(X \ge 3) = P(X = 3) = 0.064$ | 맞음. 6.4% $\ge$ 1% → 기각 실패, 맞음 |
| R10 | 13 (파생) | 없음 | $n = 3$ 에서 가능한 가장 작은 p값이 $0.064$ 다. 즉 **어떤 결과가 나와도 1%로는 기각할 수 없다.** 연승이 몇 번이어야 기각되는가: $0.4^5 = 0.01024$ ($\not< 0.01$), $0.4^6 = 0.004096$ ($< 0.01$) → 6연승 | 예제 E2의 핵심 |
| R11 | 11 텍스트 | "%50" | 50% | 오타. 전사하지 말 것 |
| R12 | 3, 5 | 15, 13시간 | 축 눈금이 있는 그림(`image11`, `image17`)에서 눈대중 확인 | 맞음(개략) |
| R13 | 4, 5, 7 | 35, 12, 13.5, 0.25, 0.5시간 | 축 눈금 없음 또는 그림과 라벨 불일치 | 재계산 불가. "슬라이드의 개략 예시"로만 인용. 새 그림은 책 자체의 예시 값으로 그리고 그렇다고 캡션에 쓴다 |

### 3.5 예제 (2~3개 + 연습 1개)

- **E1 `ex:ht-puppy`: 강아지 15마리 중 수컷 13마리(슬라이드 11).**
  $H_0$: $p = 0.5$, $H_a$: $p > 0.5$, $\alpha = 1\%$.
  1단계: 표에서 13 이상을 센다(3회) → $0.003$. 2단계: 같은 값을 이항계수로 정확히 구한다
  ($121/32768 = 0.0037$, `align*` 한 줄 한 계산, 원칙 10). 3단계: 두 값이 다른 이유(R4: 표는 반올림된
  기댓값, 14와 15 행이 0으로 반올림됨). 결론: 기각. 문장: "수컷이 더 잘 나온다는 증거가 있다."
- **E2 `ex:ht-juan`: 빨간 셔츠 복권 3연승(슬라이드 13).**
  $H_0$: $p = 0.4$, $H_a$: $p > 0.4$. $0.4^3 = 0.064$ → 기각 실패. 이어서 R10: 3번으로는 결코 기각할 수
  없고, 6연승이어야 1% 밑으로 내려간다. 교훈: 기각 실패는 "셔츠 효과가 없다"가 아니라 "이 표본 크기로는
  말할 수 없다". `\Cref{subsec:notch}` 의 "노치가 뒤집히면 표본이 부족한 것"과 같은 신호.
- **E3 `ex:ht-distance`: 합친 평균 대 집단 평균(슬라이드 9, 책 자체의 예시 값).**
  값은 슬라이드 그림 `image22/23` 모양을 흉내 낸 **책의 예시 값**임을 명시한다:
  C $= \{20, 28, 34\}$, D $= \{22, 26, 37\}$.
  - 합친 평균: $167/6 = 27.83$. 집단 평균: C $82/3 = 27.33$, D $85/3 = 28.33$.
  - 합친 평균에서의 제곱거리 합: $61.36 + 0.03 + 38.03 + 34.03 + 3.36 + 84.03 = 220.83$.
  - 집단 평균에서의 제곱거리 합: C $53.78 + 0.44 + 44.44 = 98.67$, D $40.11 + 5.44 + 75.11 = 120.67$, 합 $219.33$.
  - 줄어든 양: $220.83 - 219.33 = 1.50$ (검산: $3(27.33-27.83)^2 + 3(28.33-27.83)^2 = 0.75 + 0.75 = 1.50$).
  - 해석: 평균을 둘로 나눠도 거리가 0.7%밖에 줄지 않는다 → "극적인 차이가 없다" → 기각 실패(슬라이드 9의 문장).
    제곱을 쓰는 이유는 `\Cref{def:spread}` 의 분산과 같다고 한 줄로 연결. 정식 검정 통계량은 3.2 (5)의 열린 질문.
- **연습 E4 (본문 예제가 아니라 `pitfall` 안의 연습): 반항적 인간 14%(슬라이드 12).**
  R6~R8의 세 가지(표 읽기 $7/1000$, 구간 경계 모호성, 방향 선택)를 함정으로 보여 준다.

### 3.6 그림과 표

**그림 제작 규칙(기존 책의 관례를 따른다).** 머리말 "쓰는 방식"은 "그림은 코드로 생성한다. 손으로
그리거나 값을 타이핑하지 않는다"고 정해 두었다. 실제 파이프라인도 있다:
`hw1/00_lecture_charts/deck6_distributions.py` → `figures/06_0N_*.png`, `main.tex` 의 `\graphicspath`
가 그 폴더를 가리킨다. 기존 TikZ 그림(`fig:attr-flow`, `fig:fence-flow`, `fig:bootstrap-flow`)은 전부
흐름도/개략도이고 데이터 그림은 하나도 없다. 따라서 둘로 나눈다.

- **TikZ (개략도만, 데이터 값 없음):** 기존 스타일 이름(`flowbox`, `arrow`, 색 `bookaccent`, `bookrule`,
  `booksurface`, `bookwarn`)을 재사용한다. `pgfplots` 는 latex.md §3의 확인 목록에 없으므로 쓰지 않는다.
- **Python 스크립트 (데이터 값이 들어가는 그림):** 새 스크립트 `hw1/00_lecture_charts/deck9_hypothesis.py`
  → `figures/09_0N_*.png`(기존 `05_`, `06_` 번호 관례). 값은 스크립트 안에 슬라이드 출처 주석과 함께 한 번만
  둔다. 이 스크립트는 `developer` 몫이고 U1(본문)보다 먼저 끝나야 한다(부록 U1a). 스크립트는 그림만 그린다.
  모델 학습이나 노트북 실행은 없다.

| 라벨 | 제작 | 파일 / 내용 | 슬라이드 |
| :-- | :-- | :-- | :-- |
| `fig:ht-flow` | TikZ 흐름도 | $H_0$/$H_a$ → $\alpha$ 결정 → 방향(꼬리) → p값 → 기각/기각 실패 두 갈래. `fig:bootstrap-flow` 와 같은 세로 상자 양식 | 2, 6, 10~13 |
| `fig:ht-tails` | TikZ 개략도 | $>$ 오른쪽 꼬리, $<$ 왼쪽 꼬리, $\neq$ 양쪽 꼬리 칠하기. 종 모양은 `plot {exp(-\x*\x/2)}` 개략(축 값 없음) | 10 |
| `fig:ht-drug-experiments` | 스크립트 | `09_01_drug_experiments.png`. 점그림 2패널. 왼쪽: 큰 차이(재실험도 같은 방향, 기각). 오른쪽: 작은 차이(재실험마다 방향이 뒤집힘, 기각 실패). 책의 예시 값임을 캡션에 명시 | 3~5, 7 |
| `fig:ht-pooled-vs-group` | 스크립트 | `09_02_pooled_vs_group.png`. E3의 값. 왼쪽 합친 평균 선 하나 + 거리 선분, 오른쪽 집단 평균 선 둘 + 거리 선분. 제곱거리 합 220.83 / 219.33 을 패널에 표기 | 9 |
| `fig:ht-puppy-sim` | 스크립트 | `09_03_puppy_null.png`. 슬라이드 11 표의 빈도 $k = 0..15$ 막대, $k \ge 13$ 만 경고색, 관측값 13에 세로선. 정확한 $1000\binom{15}{k}/2^{15}$ 를 점으로 겹쳐 R4를 보이게 | 11 |
| (선택) `fig:ht-alien-null` | 스크립트 | `09_04_alien_null.png`. 슬라이드 12 빈도 + $\mathrm{Bin}(400, 0.1)$ 을 인원수 축으로. 13.5%와 14% 경계선 두 개로 R7을 보이게 | 12 |

| 라벨 | 표 |
| :-- | :-- |
| `tab:ht-symbols` | 기호 표(3.2 (1)) |
| `tab:ht-puppy` | $k$ · 슬라이드 빈도 · $1000\binom{15}{k}/2^{15}$ (소수 1자리). R4를 눈으로 보이는 표 |
| `tab:ht-alien` | 슬라이드 12 빈도 + 오른쪽 누적(위에서부터 꼬리 합) 열. 14 행의 경계 모호성을 각주로 |
| `tab:ht-wording` | 결론 문장: 올바른 표현("기각한다", "기각하지 못한다") / 틀린 표현("$H_0$ 를 채택한다", "$H_0$ 가 증명됐다") |
| `tab:ht-cases` | 사례 요약: 사례 · $H_0$ · $H_a$ · 관측 · p값 · $\alpha$ · 판정 (금성, 강아지, 외계인, 복권) |

`\Cref` 대상(기존 라벨, 존재 확인함): `sec:center`, `sec:spread`, `def:spread`, `sec:skew-compute`,
`subsec:fence`, `sec:ci`, `subsec:bootstrap`, `subsec:ci-compare`, `subsec:notch`, `ch:hw1`,
`ch:project`, `sec:binomial`(stub), `sec:normal`(stub), `tab:analogy`.

### 3.7 파일럿의 의존 관계

- `sec:binomial` 이 stub인 채로 파일럿을 써도 컴파일은 된다. 대신 파일럿의 배경 절이 이항분포를 자체
  요약해야 한다(원칙 9a). 04를 먼저 채우면 그 요약을 한 줄로 줄일 수 있다. pm이 순서를 정한다.
- `parts/part1-foundations.tex` 에 `\input` 한 줄 추가가 필요하다(2.3절).

## 4. 후속 장 요약 개요 (덱 3, 덱 7, 덱 6 → 04 채우기)

각 줄 = 절 하나. 채우기(fill)는 기존 절을 다시 쓰지 않고 **새 절만 덧붙인다**(latex.md §13).

### 4.1 덱 3 → `01-data-attributes.tex` 채우기

기존 절(`sec:attr-flow`, `sec:attr-numeric`, `sec:attr-categorical`, `sec:attr-stats`)은 유지한다.

1. (장 머리) In brief + 배경: 출처 덱 3, 선행 개념 없음(책의 첫 장).
2. `sec:attr-discrete`: 이산형(Discrete)과 연속형(Continuous). 척도 네 가지와 **다른 축**임을 2×2 표로(슬라이드 2).
3. 기존 네 절 뒤에 척도 판정 연습: 색, 경도, 만족도 1~5, 무게, 전공(슬라이드 4. 답 재확인: 명목, 구간, 순서, 비율, 명목. 모두 타당).
4. `sec:data-matrix`: 표 데이터(Tabular Data)와 $n \times d$ 데이터 행렬(Data Matrix), 행 = 객체, 열 = 속성(슬라이드 5~8). 예: 이름/급여/나이 5×3 표 → 식별자 열 제외 속성 2개(슬라이드 8. 계산 맞음). **플래그:** 슬라이드 8은 식별자 열을 열 수에 넣고, 슬라이드 11은 뺀다. 본문에서 "식별자는 속성이 아니다"로 규칙을 하나로 정한다.
5. `sec:dimension`: 차원(Dimension)의 두 뜻, 프로그래밍의 배열 차원 대 데이터 과학의 공간 차원. 100차원 데이터도 2차원 배열에 담긴다(슬라이드 9~12). 3차원 산점도 TikZ.
6. `sec:vector`: 점 = 행 = 벡터(슬라이드 13~15, 붓꽃 데이터).
7. `sec:everything-matrix`: 이미지, 텍스트(단어 동시출현), 네트워크를 행렬로 바꾸기(슬라이드 16~19). 개관 절, In brief 생략.
8. 어떻게 적용: HW1 데이터를 $126 \times 8$ 행렬로 보기(`\Cref{ex:hw1-types}`). 앞으로: 고차원(차원의 저주, 덱 1 슬라이드 4의 차원 축소).

### 4.2 덱 7 → 새 장 `lec07-qq-normalization.tex` (`ch:qq-normalization`)

1. In brief.
2. 배경: 분포(`\Cref{ch:distributions}`), 분위수 보간(`\Cref{sec:quantile}`, `\Cref{ex:quantile}`), CDF(`\Cref{sec:cdf}`).
3. 왜: "내 데이터는 어떤 분포인가"를 모르면 모수를 추정할 수 없다(슬라이드 2).
4. `sec:dist-check-hist`: 히스토그램 위에 가설 분포 겹치기(슬라이드 2~3). 그림은 04의 `06_01_normal_fit.png` 를 `\Cref` 로 가리키고 다시 그리지 않는다(원칙 12).
5. `sec:dist-check-cdf`: CDF로 한 번 더 확인(슬라이드 4). 본문은 `\Cref{sec:cdf}` 요약, 새 그림 없음.
6. `sec:qq`: Q-Q 플롯 세 단계, 정규와 균등 두 경우(슬라이드 5~10). 작은 데이터로 손 계산 예제 + TikZ 단계도. "가운데 구간은 좁고 가장자리 구간은 넓다"(슬라이드 6)를 그림으로.
7. `sec:qq-two-samples`: 두 데이터셋 비교, 표본 크기가 달라도 된다(슬라이드 5, 11).
8. `sec:discretization`: 이산화(Discretization). 등간격 경계 $v_i = \min + i \cdot (\max - \min)/k$, 등빈도 경계 $= \hat F^{-1}(i/k)$. 붓꽃 꽃받침 길이 4구간(슬라이드 13~15, 경계값은 쓸 때 재계산).
9. `sec:normalization`: 범위 정규화 $(x - \min)/\hat r$ 와 z-정규화 $(x - \hat\mu)/\hat\sigma$(슬라이드 16~17). 슬라이드 18 예제의 숫자(6000, 5700, 5700, 0.035)는 `media/image46.png` 에서 식을 복원한 뒤 재계산해야 한다(이번 사이클에서 미확인).
10. `sec:quantile-norm`: 분위수 정규화(Quantile Normalization) 손 계산(슬라이드 19~20).
11. `sec:robust-scaling`: 중앙값과 IQR로 스케일링(슬라이드 21). `\Cref{sec:spread}`, `\Cref{subsec:fence}` 연결.
12. `sec:log-transform`: 로그 변환으로 왜도 줄이기(슬라이드 22). `\Cref{sec:skewness}` 연결. 함정: 0이 있는 변수(`sugars == 0` 17개, `\Cref{ex:zero-as-missing}`)는 $\log 0$ 이 정의되지 않는다.
13. `sec:impute`: 결측값 처리, 삭제 대 대체(평균, 최빈값, 자리표시자, 예측)(슬라이드 24~25, 덱 2 슬라이드 10). `\Cref{sec:missing}` 의 "0은 결측이 아니다"를 선행 조건으로.
14. 어떻게 적용: `calories` 의 Shapiro p 값(04 stub의 TODO 값 0.00038, 쓸 때 재확인), `sugars` 로그 변환 전후 왜도.
15. 앞으로: 정규성 가정이 가설검정(`\Cref{ch:hypothesis}`)의 전제가 되는 경우.

### 4.3 덱 6 → `04-distributions.tex` 채우기

기존 절 라벨(`sec:normal`, `sec:exponential`, `sec:uniform`, `sec:binomial`, `sec:cdf`)은 유지한다.
절 순서는 덱 순서(정규 → 균등 → 지수 → 멱법칙 → 베르누이/이항)와 다르지만 파일 안 재배치는 소유자 결정 없이 하지 않는다. 새 멱법칙 절만 `sec:exponential` 뒤에 추가한다.

1. (장 머리) In brief + 배경 + 왜: 분포 = 측정값의 확률이 퍼진 모양(슬라이드 2~3). 연속/이산 목록.
2. `sec:normal` 채우기(슬라이드 4~10): 중심 = 평균, 폭 = 표준편차. 아기 키 곡선이 더 높은 이유(**플래그:** 슬라이드는 "성인 키의 가능성이 더 많다"고만 쓴다. 원리는 곡선 아래 넓이가 1이므로 폭이 좁으면 높아진다. 이 원리로 풀어 쓴다). 슬라이드 7 "±2 SD에 95%"는 근사(정확히 95.45%, 95%는 ±1.96, `\Cref{sec:ci}` 와 연결). 이동과 척도 변환. 기존 TODO(96.0%, Shapiro 0.00038)는 쓸 때 재확인.
3. `sec:uniform` 채우기(슬라이드 11~14): 주사위 $1/6$. 연속 $[0,5]$ 밀도 $1/5$, $P(1<X<2) = 1/5$, $P(4 < X < 4\tfrac13) = \tfrac13 \cdot \tfrac15 = 1/15$, $P(X=4) = 0$(모두 맞음). 평균 $(a+b)/2$, 표준편차 $(b-a)/\sqrt{12}$(슬라이드 14 그림에서 복원). 기존 `serving_size` 함정 유지.
4. `sec:exponential` 채우기(슬라이드 15~17): 사건 사이 대기 시간. 평균 $1/\lambda$, CDF $1 - e^{-\lambda x}$.
   - 노트북 예제: $\lambda = 1/5 = 0.2$, $P(X<3) = 1 - e^{-0.6} = 0.4512$. **맞음.**
   - 커피숍 예제: 평균 대기 $1/10$ 시간 = 6분. **맞음.** $P(X < 5/60) = 1 - e^{-10 \cdot 5/60} = 1 - e^{-0.8333} = 0.5654$. **슬라이드 값 0.0801은 틀렸다.** $1 - e^{-1/12} = 0.0800$ 과 일치하므로 지수에서 $\lambda$ 를 빠뜨린 것으로 보인다. 상식 검산: 평균 대기가 6분인데 5분 안에 올 확률이 8%일 수 없다.
   - **플래그:** 슬라이드 15의 "$0 < \lambda \le 1$" 은 틀렸다. $\lambda > 0$ 이면 된다. 슬라이드 17 자신이 $\lambda = 10$ 을 쓴다.
5. `subsec:geometric` (신설, `sec:exponential` 안): 기하분포(Geometric Distribution) = 분 단위로 쪼갠 지수분포(슬라이드 18). 웹사이트 첫 방문자, 평균 $\mu = 20$분 → $\lambda = 0.05$/분.
   - 슬라이드: $P(0<x<1) = 0.05$, $P(1<x<2) = 0.95 \cdot 0.05 = 0.0475$, $P(2<x<3) = 0.95^2 \cdot 0.05 = 0.045125$. "첫 1분 놓침, 둘째 1분 놓침"의 곱셈 논리로 설명.
   - 정확한 지수분포 값: $1 - e^{-0.05} = 0.04877$, $e^{-0.05} - e^{-0.10} = 0.04639$, $e^{-0.10} - e^{-0.15} = 0.04413$.
   - **플래그:** 슬라이드 값은 "매분 성공 확률 0.05인 독립 시행"이라는 기하분포 **근사**다(밀도의 시작값 $f(0) = 0.05$ 를 1분 폭에 곱함). 틀린 것이 아니라 근사임을 본문에 밝히고, 두 열을 나란히 놓은 표를 둔다. 무기억성(Memorylessness) 한 문단.
   - 가설검정 장(E2의 $0.4^3$)과 같은 "독립이면 곱한다" 규칙이므로 `\Cref{ch:hypothesis}` 로 앞뒤 연결.
6. `sec:powerlaw` (신설, 슬라이드 19~22): 파레토(Pareto), 80/20 규칙, 지수 $\alpha > 1$, $x_{\min}$, 여집합 CDF(Complementary CDF) 로그-로그 그림. 지수분포와 비교는 기존 그림 `06_06_powerlaw_vs_exponential.png`. 슬라이드 21~22가 밝힌 출처(Mark Newman, Aaron Clauset의 강의 노트)는 "슬라이드가 인용한 자료"로만 적고 bib 항목을 새로 만들지 않는다.
7. `sec:binomial` 채우기(슬라이드 23~29): 베르누이 예제 평균 $0.4 \cdot 0 + 0.6 \cdot 1 = 0.6$, 분산 $0.4(0-0.6)^2 + 0.6(1-0.6)^2 = 0.144 + 0.096 = 0.24 = p(1-p)$(**맞음**). 평균 0.6은 분포가 가질 수 없는 값(슬라이드 24). 이항: 5번 중 꼬리 2번, 특정 순서 하나 $0.5^5 = 1/32$, 순서 무관 $\binom52 / 32 = 10/32 = 0.3125$. 평균 $np$, 분산 $np(1-p)$. 가설검정 장 E1의 이항 계산이 이 절을 가리킨다.
8. `sec:cdf` 채우기: 기존 그림 `06_03_cdf.png`, 두 CDF의 최대 세로 격차 = KS 통계량 $D$. lec07이 이 절을 가리킨다(그림 한 번만).
9. 어떻게 적용: HW1 변수별 분포 판정(기존 TODO 값들). 앞으로: 모수 추정(덱 7), 분포 가정 위의 검정(덱 9).
10. 장 제목(분포, 데이터가 따르는 모양) 사이의 줄표는 기존 텍스트다. 바꿀지는 Q6.

## 5. 비유 집합 (Analogy Set)

이 책에는 이미 비유 집합이 **하나** 있다. `chapters/00-preface.tex` 의 `tab:analogy`, 주제는
"데이터 분석 = 지도 제작"이다. 본문은 `\analogy{}` 매크로로 표시한다.

| 개념 | 비유 | 쓰인 곳 |
| :-- | :-- | :-- |
| 데이터(Data) | 실제 지형 | 머리말, 03 바코드 절("지형 그 자체") |
| 요약통계(Summary Statistics) | 지도 | 02 도입, 02 왜도 함정, 03 도입 |
| 평균/중앙값(Mean/Median) | 지도의 중심점 | 머리말 |
| 퍼짐(Spread) | 지도의 축척 | 머리말 |
| 시각화(Visualization) | 항공사진 | 02 왜도 함정, 03 도입 |
| 분포 가정(Distribution) | 투영법 | 04 keyidea |

새 장은 두 번째 비유 집합을 만들지 않는다(원칙 5). 가설검정에는 같은 지도 비유를 늘려 쓴다.
**제안 행(머리말 표 수정이므로 소유자 승인 필요, Q5):**

| 개념 | 비유 | 대응의 의미 |
| :-- | :-- | :-- |
| 가설검정(Hypothesis Testing) | 현장 측량으로 지도 검증 | 지도(귀무가설)가 맞다면 이만큼 어긋난 측량이 나올 법한가를 묻는다. 너무 어긋나면 지도를 고치고(기각), 오차 안이면 고칠 근거가 없을 뿐 지도가 옳다는 증명은 아니다(기각 실패) |

파생 대응(본문에서만, 표에 추가하지 않음): p값 = "지도가 맞을 때 이만큼 어긋날 확률",
$\alpha$ = "지도를 고치기로 **측량 전에** 정한 오차 한계", 확률 계산에 쓰는 분포 = 기존의 투영법.
슬라이드의 자체 장치("$H_0$ 를 터무니없게 만드는가", 금성 외계 생명)는 비유가 아니라 예시이므로
그대로 예시로 쓴다.

## 6. 소유자 질문 목록 (Owner Questions)

- **Q1 (규칙 충돌):** book.md §3(`NN-slug`)과 원칙 12(`lecNN-slug`)가 충돌한다. 새 강의 장을 `lecNN` 로
  하는 기본안 (a)로 가도 되는가, 아니면 (b) 01~04 개명 또는 (c) 2부 개명을 원하는가(2.4절). 어느 쪽이든
  book.md의 한쪽 문장을 고쳐야 한다.
- **Q2:** 기존 장 01~04를 채울 때 원칙 12의 뼈대(장 머리 In brief, 배경, 어떻게 적용, 앞으로)를 덧붙이는가?
  권고: 예, 기존 절 본문은 건드리지 않고 앞뒤에만 추가. 03(덱 5, covered)도 같은 방식으로 할지.
- **Q3:** 덱 1(강의 계획서) skip, 덱 2(데이터란 무엇인가)는 장 없이 슬라이드 9~11만 인용. 원칙 12의 예외로 승인하는가?
- **Q4:** 덱 6, 덱 9 슬라이드 오류(4.3의 0.0801, $\lambda$ 범위; 3.4의 R4, R7, R8)를 본문에서 "슬라이드 값은 X이나
  재계산하면 Y"로 드러내는가, 조용히 올바른 값만 쓰는가? 권고: 드러낸다(`pitfall` 상자). 수업 퀴즈와 답이 어긋날 수 있기 때문이다.
- **Q5:** 머리말 `tab:analogy` 에 가설검정 행 하나를 추가해도 되는가(5절)?
- **Q6:** 기존 장의 줄표(제목 포함)를 원칙 11에 맞춰 정리하는가? 권고: 별도 과제로, 채우기 작업과 섞지 않는다.

## 부록: pm에게 넘길 설계 단위 (Design Units)

순서와 배정은 pm이 정한다. 아래는 크기와 선후 관계만이다.

| 단위 | 산출물 | 선행 | 크기 |
| :-- | :-- | :-- | :-- |
| U0 | Q1~Q6 소유자 답 | 없음 | 질문만 |
| U1a | `hw1/00_lecture_charts/deck9_hypothesis.py` → `figures/09_0N_*.png` (3.6절의 스크립트 그림). `developer` 몫 | 없음 | 소 |
| U1 | `lec09-hypothesis-testing.tex` + `part1` `\input` 한 줄 (파일럿, 3절). `sonnet-writer` 몫 | Q1, Q4, U1a | 대 |
| U2 | 04 채우기(4.3절), 특히 `sec:binomial`, `sec:exponential` | Q4 | 대 |
| U3 | `lec07-qq-normalization.tex` (4.2절), 슬라이드 18 식 복원 포함 | U2의 `sec:cdf` | 대 |
| U4 | 01 채우기(4.1절) | Q2 | 중 |
| U5 | 02 채우기(덱 4 슬라이드 4~11, 19, 20: 확률변수, PMF/PDF/CDF, 최빈값, 범위) | Q2 | 중 |

**CDF 경계:** CDF의 **정의**(확률변수, PMF/PDF/CDF, 주사위 예제)는 02(덱 4)에만 둔다. 04의 `sec:cdf` 는
정의를 다시 쓰지 않고 `\Cref` 로 02를 가리킨 뒤 **비교 도구**(두 CDF의 최대 격차, KS $D$)만 다룬다.
lec07은 04의 `sec:cdf` 를 가리킨다. 한 개념 = 한 정의 자리.

**데이터 그림 공통:** lec07(Q-Q 예제, 이산화 경계, 정규화 전후)과 04 채우기의 새 데이터 그림도 3.6절 규칙을
따른다. 즉 `deck7_*.py` / 기존 `deck6_distributions.py` 확장이 선행 단위다(pm이 U2, U3 앞에 `developer`
단위로 분리).
| U6 | 머리말 비유 행 추가 | Q5 | 소 |

각 단위의 공통 수용 기준 후보: 줄표 0개(em dash 문자와 LaTeX 하이픈 세 개를 grep으로 찾아 새 줄에서 0건), 모든 새 절에 `inbrief`,
3.4 / 4.3의 재계산 값과 본문 숫자 일치, `latexmk -xelatex main.tex` 경고 없는 `\Cref` 해석, 모델 학습이나
노트북 실행 없음(R6의 `scipy` 한 줄은 허용 범위의 계산).

## 7. 덱 10, 11 추가 (2026-09-23, system-architect, cycle 1/2)

PLAN.md §6 2단계에 따른 덧붙임이다. 위 0~6절과 부록은 고치지 않는다. 위 내용과 달라지는 곳은
이 절 안에서 "대체(supersedes)"라고 명시한다.

### 7.1 커버리지 행 (1절 표에 이어지는 행)

| 덱 | 슬라이드 수 | 기존 장 | 상태 | 조치 | 근거 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 10_P-value | 25 | `lec09` 가 p값 정의(`def:pvalue`)와 1% 기준만 다룸 | partial (겹침 1장) | **new chapter `lec10`** | 슬라이드 2만 덱 9(약 C/D, 귀무가설 정의)의 반복이다. 슬라이드 22~25는 덱 11의 예고(덱 11 슬라이드 1, 2, 6, 8과 같은 그림 `image49` = 덱 11 `image6`, `image50` = 덱 11 `image7`)다. 1, 21은 내용 없음. **새 내용은 슬라이드 3~20의 18장**: 재판 비유, 표본 크기와 p값, 0.05 기준과 1종 오류, 효과 크기, 세 부분 p값(이산), 연속 p값, 시뮬레이션 대 모수적 계산(z), 신뢰구간과 검정의 쌍대성 |
| 11_Comparing_Distributions | 24 | 없음. `hw02` 가 KS 거리 $D$ 를 이미 씀, `04` `sec:cdf` 는 stub | none | **new chapter `lec11`** | t검정(단일, 두 표본), t표 읽기, ANOVA의 F검정, KS 검정(한 표본, 두 표본)이 전부 미수록. 24는 Kahoot |

### 7.2 결정 사항

**결정 1. 덱 10은 새 장 `chapters/lec10-p-value.tex` 로 한다(`lec09` 채우기 아님).**
- 슬라이드 25장 중 반복 1장(2), 덱 11 예고 4장(22~25), 빈 슬라이드 2장(1, 21), **새 내용 18장(3~20)**.
  18장이면 원칙 12의 여섯 단계 뼈대를 채우고도 남는다. "너무 얇으면 채우기"라는 예외 조건에 해당하지 않는다.
- `lec09` 는 막 리뷰 수정을 마친 장이다(커밋 `74f1192`). 18장 분량을 그 안에 넣으면 latex.md §13(최소 diff)과
  원칙 12(노트 하나 = 장 하나)를 동시에 어긴다.
- 반복 처리: 슬라이드 2는 `lec10` 배경 절에서 한 줄 요약 + `\Cref{def:ht-null,def:ht-alt}`. 슬라이드 22~25는 `lec10` 에
  쓰지 않고, `lec10` 의 "앞으로" 절에서 `\Cref{ch:compare-dist}` 로 넘긴다. 그 내용의 본문은 `lec11` 이 한 번만 쓴다(원칙 12 마지막 항).
- **p값을 다시 정의하지 않는다.** 정의는 `def:pvalue`(lec09) 한 곳이다. `lec10` 은 그 정의를 **넓힌다**: 세 부분 분해,
  연속 데이터, 기준의 의미(1종 오류), 효과 크기와의 구분, 시뮬레이션과 모수적 계산, 신뢰구간과의 관계.
- 필수 `pitfall`(정의가 둘로 보이는 것을 막는다): 슬라이드 10의 "세 부분" p값은 **양측** p값이다. `lec09` 의 강아지,
  복권 예제는 단측($H_a: >$)이라 둘째 부분("똑같이 드문 다른 결과")이 빠진다. 세 부분 = 정의 하나를 양측에 적용한 것.

**결정 2. 덱 11은 새 장 `chapters/lec11-comparing-distributions.tex`, 라벨 `ch:compare-dist`.**
- 연결(모두 존재 확인):
  - `hw02`: `sec:hw2-compare-how` 의 "QQ 도표와 $D$" 단락이 KS 거리를 이미 쓴다. 단 `hw02` 의 $D$ 는 데이터 ECDF 대
    **모형에서 뽑은 표본**의 ECDF이므로 정확히는 슬라이드 21~22의 **두 표본 KS** 다. `lec11` 의 두 표본 절이 이 점을 짚고
    `\Cref{sec:hw2-compare-how}`, `\Cref{tab:hw2-airport-verdict}`, `\Cref{tab:hw2-movie-verdict}` 를 가리킨다.
    `sec:hw2-learned` 의 "KS 거리 하나로는 왜 지는지 알 수 없다"는 슬라이드 23("KS-statistic itself is usually not
    informative")과 같은 말이므로 거기서도 연결한다.
  - `04`: `sec:normal` 안 함정(같은 표본으로 모수를 추정한 뒤 KS를 하면 p값이 낙관적). 이 함정에는 라벨이 없으므로
    `\Cref{sec:normal}` 로 가리킨다. 슬라이드 15~19의 예제는 모수가 **미리 정해진** $N(0,1)$ 이라 이 함정에 걸리지 않는다는
    대조를 쓴다. `sec:cdf`(stub)는 아래 결정 5.
  - `03`: `fig:violin` 캡션의 보스턴 마라톤은 슬라이드 2의 질문과 같은 데이터다. `subsec:ci-compare` 함정의 "정확한 판정에는
    별도의 검정이 필요하다"가 바로 두 표본 t검정이다.
  - `lec09`: `sec:ht-future` 가 열어 둔 질문 두 개를 이 장이 닫는다. (a) "평균에서의 거리를 정식 검정 통계량으로"
    = ANOVA의 SSB/SSW, (b) "정규성 가정이 필요한 검정" = t검정. `ex:ht-distance` 의 값을 ANOVA 예제로 다시 쓴다(7.4.5 E5, `ex:cd-revisit`).

**결정 3. 배치: `parts/part1-foundations.tex` 에서 `lec09` 바로 뒤, 덱 번호 순.**

```
\input{chapters/04-distributions}          % 덱 6
\input{chapters/lec07-qq-normalization}    % 덱 7 (계획, 파일 없음)
\input{chapters/lec09-hypothesis-testing}  % 덱 9
\input{chapters/lec10-p-value}             % 덱 10
\input{chapters/lec11-comparing-distributions} % 덱 11
```
루트 CLAUDE.md(2026-09-23 저자 결정)대로 순서의 권위는 `\input` 순서다. `hw02` 는 2부에 그대로 둔다. `lec11` 이 1부이고
`hw02` 가 2부이므로 `hw02` 에서 `lec11` 로의 역참조는 "앞 장 참조"가 되어 읽는 순서와 맞다. `lec07` 은 아직 파일이
없으므로 그 줄은 U3 때 넣는다. `lec10`, `lec11` 은 `lec07` 유무와 무관하게 컴파일된다(`ch:qq-normalization` 을 가리키지 않는다).

**결정 4. 슬라이드 번호와 오류.** 번호는 `slides.md` 의 `## Slide N` 을 따른다. 오류와 모호성은 7.3.4, 7.4.4 재계산 표에
R 번호로 적었다. 요약: 덱 10은 백분율 두 개(29%, 0.001%), 귀무가설 방향 뒤집힘(슬라이드 13), 경계 p값(0.05를 유의로 판정),
연구 질문과 $H_a$ 방향 불일치(16), 관측 평균 62 대 63(15, 17, 18), 신뢰구간 해석과 결론 문장(20). 덱 11은 단일 표본
t 공식의 $\sqrt n$, $\mu_0$ 누락(3), "Comparing Variances" 제목과 ANOVA의 실제 대상(평균) 불일치(2, 10), 자유도 라벨(11),
KS 통계량의 한쪽 격차만 계산(19), 두 표본 $F_y$ 반올림(21), "Comparing to Samples" 오타(21, 22), 한 표본용 임계값표를
두 표본에 쓸 수 없고 두 표본 예제는 점근 p(약 0.08)와 정확 p(약 0.050)가 판정을 가름(23).

**결정 5 (대체, supersedes 부록 "CDF 경계" 단락의 KS 부분). KS 정의의 자리는 `lec11` 이다.**
부록은 "04의 `sec:cdf` 가 두 CDF의 최대 격차, KS $D$ 를 다룬다"고 적었다. 덱 11이 KS를 정식 검정으로 가르치므로
한 개념 = 한 정의 자리 원칙에 따라 **정의 `def:ks-statistic` 과 검정은 `lec11` `sec:cd-ks` 가 소유**한다. 04의 `sec:cdf` 는
CDF 두 개를 겹친 그림(`06_03_cdf.png`)과 "최대 세로 격차를 KS 통계량이라 부른다" 한 줄 + `\Cref{sec:cd-ks}` 만 둔다.
`hw02` 는 이미 `\Cref{sec:cdf}` 를 쓰므로 고칠 필요가 없다(링크 사슬 `hw02 → sec:cdf → sec:cd-ks`). 4.3절 8항과 U2의
범위가 이만큼 줄어든다. pm이 U2 수용 기준에 반영한다.

### 7.3 덱 10 → 새 장 `lec10-p-value.tex` 개요

- 파일 `chapters/lec10-p-value.tex`, 라벨 `ch:pvalue`, 절 라벨 접두사 `sec:pv-`.
- 장 제목(줄표 없음): `p값: 우연이라면 이 정도 결과가 얼마나 드문가`
- 출처: `notes/10_P-value.pptx`(25 슬라이드). 외부 문헌 표기 없음. 그림 속 예제(약 A/B 표, 동전, 브라질 여성 키, 비타민 D,
  배 칼로리, 토마토)는 슬라이드 자체를 출처로 적고 bib 항목을 만들지 않는다.
- 환경: 기존 `inbrief`, `keyidea`, `pitfall`, `definition`, `example`. 새 매크로 없음.
- 비유: 기존 지도 비유를 늘려 쓴다(5절). 슬라이드 3의 "개의 재판"(무죄 추정)은 비유가 아니라 **예시**로 쓴다. 두 번째 비유
  집합을 만들지 않는다(원칙 5). `lec09` 의 `pit:ht-fail-not-proof` 가 이미 "무죄를 증명하지 않는다"는 말을 썼으므로 그 문장을
  재판 예시와 연결한다.

#### 7.3.1 장 머리 In brief

- **Why:** p값(p-value)은 "우연만으로 이 정도 결과가 나올 확률"을 숫자 하나로 준다. 그 숫자를 제대로 읽지 못하면 검정 결론을 거꾸로 쓴다.
- **When:** 귀무가설(Null Hypothesis)을 세운 뒤, 관측한 데이터가 그 가설 아래에서 얼마나 드문지 판정할 때.
- **Where:** 두 약의 완치율 비교, 동전의 공정성, 한 사람의 키가 모집단에서 튀는지, 평균 비타민 D가 기준값과 다른지.
- **How:** 관측값과 똑같이 드문 결과, 더 드문 결과의 확률을 모두 더한다 → 미리 정한 기준(보통 0.05)과 비교한다 → p값은 차이의 **크기**가 아니라 차이의 **확실성**만 말한다는 점을 결론 문장에 반영한다.

#### 7.3.2 절 구성

**(1) 배경 `sec:pv-background`** (개념 절 아님, In brief 생략)

| 선행 개념 | 어디서 다뤘나 | 처리 |
| :-- | :-- | :-- |
| 귀무가설, 대립가설, 기각/기각 실패 | `\Cref{def:ht-null,def:ht-alt}`, `\Cref{sec:ht-decision}` | 한 줄 요약 + 링크. 슬라이드 2(덱 9 반복)는 여기서 끝낸다 |
| p값의 정의 | `\Cref{def:pvalue}` | 정의를 **인용만** 한다. 다시 쓰지 않는다 |
| 단측/양측 | `\Cref{sec:ht-forms}`, `\Cref{fig:ht-tails}` | 한 줄 + 링크. 세 부분 p값 함정의 전제 |
| 곱셈 규칙, 이항계수 | `\Cref{sec:ht-background}`, `\Cref{ex:ht-puppy}` | 한 줄. 동전 5번의 $\binom{5}{k}/32$ 에 필요 |
| 정규분포, 곡선 아래 넓이 = 확률 | `\Cref{sec:normal}`, `\Cref{def:normal}` | 두 문장. 04가 아직 stub이므로 "넓이가 곧 확률"을 이 자리에서 직접 설명 |
| 표준오차(Standard Error) | `\Cref{pit:sd-se-ci}` | 한 줄 + 링크. 비타민 D의 3.3이 표준오차다 |
| 신뢰구간 | `\Cref{sec:ci}`, `\Cref{subsec:ci-compare}` | 한 줄 + 링크. 7절(쌍대성)의 전제 |

기호 표 `tab:pv-symbols`: $\alpha$(유의수준), p값(한글 단어로만, `lec09` 의 규칙 유지), $z$(표준화 점수), $\mathrm{SE}$(표준오차),
$\mu_0$(귀무가설이 정한 값). `lec09` 의 `tab:ht-symbols` 를 `\Cref` 로 가리키고 새 기호만 싣는다.

**(2) 왜 필요한가 `sec:pv-why`** (슬라이드 3, 4)

- 문제: 한 사람에게 약 A, 한 사람에게 약 B를 주고 A만 나았다(슬라이드 4 `image3`, `image4`). "A가 낫다"고 말할 수 없다. 두 명씩
  줘도 마찬가지다(`image5`, `image6`). 우연히 그럴 수 있기 때문이다.
- 반대 극단: 1,046명 중 1,043명 완치 대 1,434명 중 2명 완치(`image7`, `image8`). 이것이 우연이라고 믿기는 어렵다.
- 그 사이: 198명 중 73명 대 190명 중 59명(`image9`, `image10`). 판단이 서지 않는다. **"얼마나 확신하는가"를 숫자로 줄 도구가 필요하다.** 그것이 p값이다(슬라이드 4: 0.24).
- 처방의 직관(슬라이드 3): "개가 무죄인 세상"을 만들고, 그 세상에서 지금 증거만큼 나쁜 일이 일어날 확률을 묻는다. 작을수록 무죄 가정이 터무니없어진다.
- 표 `tab:pv-drug-trials`(아래 7.3.6)가 세 경우와 슬라이드 5, 7의 표를 한곳에 모은다.

**(3) 무엇인가: 세부 절**

| 절 | 라벨 | In brief 초안 (Why / When / Where / How) | 슬라이드 |
| :-- | :-- | :-- | :-- |
| 기준 0.05와 거짓 양성 | `sec:pv-threshold` | Why: "얼마나 작아야 작은가"에 답이 없으면 판정이 사람마다 달라진다. / When: 데이터를 보기 전에 기준을 정할 때. / Where: 약 A 대 약 A처럼 실제 차이가 없는 비교. / How: 기준 0.05는 "차이가 없을 때 실험 100번 중 약 5번은 잘못 기각한다"는 약속이다. 이 잘못을 거짓 양성(False Positive), 1종 오류(Type I Error)라 부른다. | 5, 6 |
| p값은 효과 크기가 아니다 | `sec:pv-effect-size` | Why: 작은 p값을 "큰 차이"로 읽는 실수가 가장 흔하다. / When: 결론 문장을 쓸 때. / Where: 약 A 34% 대 약 B 35%, 표본 약 29,000명. / How: p값과 함께 차이 자체(효과 크기, Effect Size)를 따로 적는다. | 7 |
| 이산 데이터의 p값: 세 부분 | `sec:pv-discrete` | Why: "관측값이 나올 확률"만 보면 모든 결과가 드물어 보인다. / When: 결과를 셀 수 있을 때(동전). / Where: 동전 2번, 5번. / How: 관측값의 확률 + 똑같이 드문 다른 결과의 확률 + 더 드문 결과의 확률. | 8~12 |
| 연속 데이터의 p값: 넓이 | `sec:pv-continuous` | Why: 연속값에서는 한 점의 확률이 0이라 "관측값의 확률"을 셀 수 없다. / When: 키, 농도 같은 측정값. / Where: 브라질 여성 키. / How: 관측값보다 극단적인 쪽의 곡선 아래 넓이를 더한다(양측이면 양쪽). | 13, 14 |
| 시뮬레이션과 모수적 계산 | `sec:pv-sim-vs-param` | Why: 귀무가설 아래 분포를 알아야 넓이를 잰다. 그 분포를 만드는 길이 두 가지다. / When: 표준오차를 알 때. / Where: 비타민 D 평균 대 기준 100. / How: (a) 귀무가설 세상에서 연구 10,000번을 모의해 관측값 이상을 센다, (b) 정규분포를 알면 $z = (\bar x - \mu_0)/\mathrm{SE}$ 로 바로 계산한다. | 16, 17, 18 |
| 신뢰구간과 가설검정 | `sec:pv-ci-duality` | Why: 같은 데이터에서 두 도구가 서로 다른 말을 하면 안 된다. / When: 신뢰구간이 이미 있을 때. / Where: 비타민 D 95%, 99% 구간, 배 두 품종. / How: 유의수준 = 1 − 신뢰수준. 귀무가설의 값이 구간 밖이면 기각, 안이면 기각 실패. 구간은 p값보다 정보가 많다(크기와 불확실성을 함께 준다). | 15, 19, 20 |

절 안의 `pitfall` (각 절에 하나씩, 필수):
- `sec:pv-threshold`: 기준 0.05는 임의다(슬라이드 6 "The threshold is arbitrary"). `\Cref{pit:ht-alpha}` 와 같은 함정이므로
  새 함정을 만들지 않고 한 줄 + 링크로 끝낸다. 대신 새 함정 하나: "p값 0.24는 '24% 확률로 차이가 없다'는 뜻이 아니다."
- `sec:pv-effect-size`: 슬라이드 7. 34% 대 35%는 p ≈ 0.04로 "유의"하지만 차이는 1%포인트다.
- `sec:pv-discrete`: **세 부분 = 양측**(7.2 결정 1). 강아지(`\Cref{ex:ht-puppy}`)와 복권(`\Cref{ex:ht-juan}`)은 단측이므로 둘째
  부분이 없다. 표로 대조: 동전 5번 양측 0.375, 같은 관측의 단측($H_a$: 앞면이 더 잘 나온다) $6/32 = 0.1875$.
- `sec:pv-continuous`: 슬라이드 13의 귀무가설 문장이 거꾸로다(R10). 올바른 문장: "$H_0$: 이 사람은 이 분포에서 나왔다(특별하지 않다)".
  p값이 정확히 0.05이면 "0.05보다 작다"가 아니므로 엄밀히는 기각 실패다(R11).
- `sec:pv-ci-duality`: 슬라이드 20의 결론 문장 "사과(배)의 평균 칼로리는 같다"는 `\Cref{tab:ht-wording}` 의 틀린 표현이다(R19).
  "100번 계산하면 99번 이 구간 안에" 도 틀린 해석이다. 올바른 해석: 같은 절차로 구간을 100번 만들면 약 99개가 참값을 포함한다.

**(4) 어떻게 적용하는가 `sec:pv-apply`**

- 결론 문장 틀 하나: "[p값]이므로 [기각/기각 실패]. 차이는 [효과 크기, 신뢰구간]이다." `\Cref{fig:ht-flow}` 다섯 단계의 마지막에 붙인다.
- 이 책 안의 적용 자리:
  - `\Cref{subsec:ci-compare}`, `\Cref{fig:ci-compare}`: 버거 대 치킨너겟의 캡션 "$p < 0.05$" 가 쌍대성의 한 사례임을 짚는다. 새 수치 없음.
  - `\Cref{pit:sd-se-ci}`: 표준오차 대 표준편차. 비타민 D의 3.3은 표준편차가 아니라 표준오차라서 $z$ 가 11이나 된다.
  - `\Cref{ch:hw1}`: 과제 데이터로 결론 문장 틀을 채워 보는 연습(계산 없이 문장만).
  - `\Cref{ch:project}`: 결과 절에서 p값과 효과 크기를 함께 적는 점검 목록.
- 확인 문제(슬라이드 21 Kahoot 대신) 3개: 동전 3번 중 앞면 3번의 양측 p값($2/8 = 0.25$), p값 0.04와 차이 1%포인트 해석, 95% 구간이 0을 포함할 때 $\alpha = 0.05$ 결론.

**(5) 앞으로 `sec:pv-future`**

- 두 집단 평균을 비교하는 정식 검정(t검정)과 분포 전체 비교(KS 검정): 슬라이드 22~25가 예고한다. `\Cref{ch:compare-dist}`.
- 2종 오류(Type II Error)와 검정력(Power): 슬라이드 5는 1종 오류만 이름 붙인다. `lec09` `sec:ht-future` 의 검정력 질문과 같은 갈래로 적는다.
- 여러 번 검정할 때(유전자 수천 개, 덱 11 슬라이드 2의 암 유전자 질문) 기준 0.05가 만드는 거짓 양성의 누적. 덱에 없으므로 질문으로만.

#### 7.3.3 슬라이드 → 절 대응 (모든 슬라이드)

| 슬라이드 | 내용 | 쓰는 절 | 비고 |
| :-- | :-- | :-- | :-- |
| 1 | 제목 | (장 제목) | 내용 없음 |
| 2 | 약 C/D, 귀무가설, 대립가설, 유의성 검정 정의 | `sec:pv-background` | **덱 9 슬라이드 5, 6의 반복.** 한 줄 + `\Cref{def:ht-null,def:ht-alt}` |
| 3 | 개의 재판: 무죄인 세상, "적어도 이만큼 나쁜" 확률 | `sec:pv-why` | `image2.png` 은 창문으로 들어가는 사람 사진. 재현하지 않는다. `\Cref{pit:ht-fail-not-proof}` 연결 |
| 4 | 약 A/B, 1명씩, 2명씩, 대규모 두 표, p = 0.24 | `sec:pv-why` | R1~R3. 표 `tab:pv-drug-trials` |
| 5 | 기준 0.05, 약 A 대 약 A, P = 1, P = 0.01, 1종 오류 | `sec:pv-threshold` | R4, R5 |
| 6 | 0.05 = 차이 없을 때 5%가 거짓 양성, 기준은 임의 | `sec:pv-threshold` | `\Cref{pit:ht-alpha}` |
| 7 | p값은 차이의 크기를 말하지 않는다 | `sec:pv-effect-size` | R6. `image15` |
| 8 | 내 동전은 특별한가, p값 = 확률의 합 | `sec:pv-discrete` | `image16`, `image17` 은 장식 |
| 9 | 동전 2번: HH 1/4, TT 1/4, HT/TH 2/4 | `sec:pv-discrete` | R7. `image18` 수형도는 TikZ로 다시 그린다 |
| 10 | 세 부분, 0.25 + 0.25 + 0 = 0.5 | `sec:pv-discrete` | R7 |
| 11 | 세 부분의 꽃 비유 | `sec:pv-discrete` | 사진(`image20~26`)은 재현하지 않는다. "흔한 꽃 여럿이 모두 똑같이 드물면 한 송이는 특별하지 않다"를 문장으로만 |
| 12 | 동전 5번 앞면 4번, 5/32 + 5/32 + 2/32 = 0.375 | `sec:pv-discrete` | R8. 예제 E2 |
| 13 | 브라질 여성 키, 95%가 142~169 cm, 142 cm의 p값 0.05 | `sec:pv-continuous` | R9~R11. 예제 E3 |
| 14 | 155.4~156 cm의 p값 0.04 + 0.48 + 0.48 = 1 | `sec:pv-continuous` | R12 |
| 15 | 비타민 D 신뢰구간, 62 ± 2 × 3.3 | `sec:pv-ci-duality` | R13. `image36` |
| 16 | 비타민 D 연구 질문, 귀무가설 100 | `sec:pv-sim-vs-param` | R14(방향) |
| 17 | 시뮬레이션 10,000번, 63은 한 번도 안 나옴, p < 1/10,000 | `sec:pv-sim-vs-param` | R15. `image37~40` |
| 18 | 모수적 계산 $z = (63-100)/3.3$ | `sec:pv-sim-vs-param` | R15, R16 |
| 19 | 신뢰구간 대 검정, 유의수준 = 1 − 신뢰수준 | `sec:pv-ci-duality` | R17. `image42`, `image43` |
| 20 | 배 두 품종 99% 구간 $4 \pm 6.44$ | `sec:pv-ci-duality` | R18, R19. 예제 E5 |
| 21 | Kahoot | `sec:pv-apply` 확인 문제 | 내용 없음 |
| 22 | Comparing Distributions 제목 | `sec:pv-future` 한 줄 → `lec11` | 덱 11 슬라이드 1과 같음 |
| 23 | 무엇을 비교하나(평균, 분산, 분포) | `lec11` `sec:cd-choose` | 덱 11 슬라이드 2와 같음. 덱 10에만 "Different variables, from different populations?" 한 줄이 더 있다. `lec11` 에서 이 물음표 줄을 "이 경우는 비교 자체가 성립하는지부터 물어야 한다"로 다룬다 |
| 24 | 두 표본 t검정 공식 | `lec11` `sec:cd-t-two` | `image49` = 덱 11 `image6` |
| 25 | 토마토 예제, p < 0.05면 기각 | `lec11` `sec:cd-t-two` | `image50~56` = 덱 11 슬라이드 8. 재계산은 7.4.4 R5 |

#### 7.3.4 재계산 표 (전사 금지)

| # | 슬라이드 | 슬라이드 값 | 재계산 | 판정 |
| :-- | :-- | :-- | :-- | :-- |
| R1 | 4 `image9` | 73/(73+125), "37%" | $73/198 = 0.3687$ → 36.9% | 맞음 |
| R2 | 4 `image10` | 59/(59+131), "29%" | $59/190 = 0.3105$ → **31.1%** | **오류.** 본문은 31%를 쓰고 `pitfall` 에서 슬라이드 값을 밝힌다(Q4 방침) |
| R3 | 4 `image7`, `image8` | "99.7%", "0.001%" | $1043/1046 = 0.99713$ → 99.7%. $2/1434 = 0.001395$ → **0.14%** | 99.7% 맞음. **0.001%는 오류**(비율 0.0014를 %로 착각한 것으로 보인다) |
| R3a | 4 | p = 0.24 | 2×2 표 [[73,125],[59,131]]. 카이제곱(보정 없음) $\chi^2 = 1.461$, p = 0.227. Yates 보정 $\chi^2 = 1.214$, p = 0.271 | 0.24는 두 값 사이. Fisher 정확검정 값으로 보인다. `scipy.stats.fisher_exact([[73,125],[59,131]])` 한 줄로 확인(초 단위, 학습 아님). 본문은 "약 0.24"로 쓰고 검정 이름은 이 장에서 가르치지 않는다 |
| R4 | 5 `image13`, 텍스트 | P = 1, 텍스트 "P-value = 0.9" | [[71,127],[72,126]]: $\lvert ad-bc\rvert = 198 = N/2$ 라 Yates $\chi^2 = 0$ → p = 1. [[73,125],[71,127]](`image11` 대 `image12`): Yates p = 0.917 ≈ 0.9 | 둘 다 맞음. **모호:** 텍스트의 0.9가 어느 쌍인지 슬라이드가 밝히지 않는다. 본문은 쌍을 명시한다 |
| R5 | 5 `image14` | 30%, 42%, P = 0.01 | $60/198 = 30.3\%$, $84/198 = 42.4\%$. 보정 없는 $\chi^2 = 6.29$, p = 0.012. Yates p = 0.016 | 맞음(약 0.01). 약 A 대 약 A인데 p가 작다 = 거짓 양성의 실례 |
| R6 | 7 `image15` | 34%, 35%, p = 0.04 | $5005/14873 = 33.65\%$, $4800/13800 = 34.78\%$. $\chi^2 = 4.07$, $z = 2.02$, p = 0.044 | 맞음. 차이 1.1%포인트 |
| R7 | 9, 10 | 1/4, 2/4, 1/4, p = 0.5 | $0.5^2 = 0.25$, HT와 TH 두 경우 $2 \times 0.25 = 0.5$. p = 0.25(HH) + 0.25(TT, 똑같이 드묾) + 0(더 드문 것 없음) = 0.5 | 맞음 |
| R8 | 12 | 5/32 + 5/32 + 2/32 = 12/32 = 0.375 | $2^5 = 32$. $\binom{5}{4} = 5$(앞4 뒤1), $\binom{5}{1} = 5$(앞1 뒤4, 똑같이 드묾), $\binom{5}{5} + \binom{5}{0} = 2$(더 드묾). 합 $12/32 = 0.375$ | 맞음. 단측이면 $(5+1)/32 = 0.1875$(7.3.2 함정) |
| R9 | 13 `image29`, `image33` | 95%가 142~169 cm, 평균 155.7 | 구간 중점 $(142+169)/2 = 155.5$ ≈ 155.7(반올림 차). $\sigma \approx (169-142)/(2 \times 1.96) = 6.89$ cm | 맞음(개략). **$\sigma$ 는 슬라이드에 없는 책의 유도값**이라고 밝힌다 |
| R10 | 13 텍스트 | "Null Hypothesis: This person is special from this distribution" | 귀무가설은 "특별하지 않다(이 분포에서 나왔다)"여야 한다 | **오류(방향 뒤집힘).** 기각하면 그때 "특별하다" |
| R11 | 13 | p = 0.025 + 0.025 = 0.05, "statistically significant" | 양측 p = 0.05. `def:pvalue` 와 `lec09` 의 규칙은 "p < α 이면 기각" | **플래그(경계):** 0.05는 0.05보다 작지 않으므로 엄밀히는 기각 실패. 142 cm는 정확히 경계선 위의 값이라는 점을 본문에서 짚는다 |
| R12 | 14 | 0.04 + 0.48 + 0.48 = 1 | 봉우리 밀도 $1/(6.89\sqrt{2\pi}) = 0.0579$/cm × 폭 0.6 cm = 0.035 ≈ 0.04. $P(h < 155.4) = \Phi(-0.3/6.89) = \Phi(-0.044) = 0.483$, 대칭으로 $P(h > 156) = 0.483$. 합 1.000 | 맞음. 평균을 포함하는 구간의 양측 p값은 언제나 1이라는 점이 교훈 |
| R13 | 15 `image36` | $62 \pm 2 \times 3.3 = 55.4$ ~ $68.6$ | $62 - 6.6 = 55.4$, $62 + 6.6 = 68.6$. 정확히 1.96이면 $62 \pm 6.47 = [55.53, 68.47]$ | 맞음. "2는 1.96의 반올림"은 슬라이드 자신이 적었다. `\Cref{sec:ci}` 의 1.96과 연결 |
| R14 | 16 | 연구 질문 "100보다 **낮은가**", $H_a$ "100이 **아니다**" | 질문은 단측($<$), 대립가설은 양측($\neq$) | **플래그(방향 불일치).** 덱 9 R8과 같은 종류. `\Cref{pit:ht-direction}` 으로 연결. 결론은 어느 쪽이든 기각 |
| R15 | 15, 17, 18 | 관측 평균 62(`image36`), 63(`image39`, `image41`) | 두 값이 섞여 있다 | **플래그(불일치).** 본문은 63 하나로 통일하고 각주로 밝힌다(62로 해도 $z = -11.5$, 결론 동일) |
| R16 | 18 `image41` | $z = (63-100)/3.3 = 11.2$ | $-37/3.3 = -11.21$ | **부호 누락.** $\lvert z \rvert = 11.2$. p는 $10^{-28}$ 보다 작다(정규 꼬리). 슬라이드 "p << 0.0001" 은 맞음. 같은 슬라이드의 "true average weight"는 "비타민 D 평균"의 오타 |
| R17 | 19 `image42`, `image43` | 95% 구간(약 55~70), 99% 구간(약 53~72), 귀무값 100, p < 0.05, p < 0.01 | 99% 구간 $62 \pm 2.576 \times 3.3 = [53.5, 70.5]$. 100은 두 구간 밖 | 맞음(그림은 개략). "p값은 구간에서 언제나 얻을 수 있다"는 같은 모형($\text{추정값} \pm z \cdot \mathrm{SE}$)일 때만 참이라는 조건을 붙인다 |
| R18 | 20 `image44` | Bosc 120, 15, $n = 65$, SE 1.86. Anjou 116, 13, 65, SE 1.61. 99% 구간 $4 \pm 6.44$ | $15/\sqrt{65} = 1.860$, $13/\sqrt{65} = 1.612$. $\mathrm{SE}_{\text{diff}} = \sqrt{1.860^2 + 1.612^2} = 2.462$. $6.44 / 2.462 = 2.616$ = t분포(자유도 약 124)의 99% 값. $z$ 를 쓰면 $2.576 \times 2.462 = 6.34$ | 맞음. 구간 $[-2.44, 10.44]$ 가 0을 포함 → p > 0.01, 기각 실패. 참고: $z = 4/2.462 = 1.62$, 양측 p ≈ 0.11 |
| R19 | 20 텍스트 | "If we compute this parameter 100 times, 99 times the difference will be in this interval", "P < 0.01 → reject → the pears are different", "Apples are the same" | 가정법 두 갈래(겹치지 않았다면 / 실제로는 0 포함)가 한 슬라이드에 섞였다 | **플래그 셋:** (i) 구간 해석이 틀렸다(7.3.2 함정), (ii) "Apples"는 "pears"의 오타, (iii) "the same"은 `\Cref{tab:ht-wording}` 의 틀린 표현. 올바른 결론: "평균 칼로리가 다르다고 말할 근거가 없다" |

#### 7.3.5 예제

- **E1 `ex:pv-drug`: 약 A 대 약 B(슬라이드 4).** 1명씩 → 2명씩 → 1,000명대 → 약 200명씩. 같은 질문이 표본 크기에 따라 확신이
  달라진다. 비율은 R1~R3의 재계산 값. p값은 "약 0.24"(R3a)로만 쓰고 계산은 하지 않는다(검정 이름이 이 덱에 없다).
- **E2 `ex:pv-coin5`: 동전 5번 중 앞면 4번(슬라이드 12).** 32가지 결과를 앞면 수로 묶는다(1, 5, 10, 10, 5, 1). 세 부분을
  한 줄에 하나씩 `align*`. 이어서 단측 0.1875와 대조(7.3.2 함정). 동전 2번(슬라이드 9, 10)은 E2 앞의 준비 예로 짧게.
- **E3 `ex:pv-height`: 브라질 여성 키(슬라이드 13, 14).** 142 cm의 양측 p = 0.05(경계, R11), 155.4~156 cm의 p = 1(R12).
  $\sigma = 6.89$ 가 책의 유도값임을 밝힌다.
- **E4 `ex:pv-vitd`: 비타민 D(슬라이드 15~18).** 시뮬레이션 10,000번 중 0번 → p < 1/10,000. 모수적 $z = -11.2$.
  두 방법이 같은 결론에 이르는 이유(표본 평균의 분포가 정규에 가깝다). `\Cref{sec:ht-simulation}` 과 `\Cref{sec:ht-exact}`
  의 짝(시뮬레이션 대 정확 계산)이 연속 데이터에서도 그대로라는 점을 연결한다. 방향 문제 R14는 `pitfall`.
- **E5 `ex:pv-pears`: 배 두 품종(슬라이드 20).** 구간에서 결론 읽기. R18 값. 결론 문장은 R19의 올바른 형태.

#### 7.3.6 그림과 표

그림 규칙은 3.6절 그대로: 값이 들어가는 그림은 스크립트, 개략도는 TikZ. 새 스크립트
`hw1/00_lecture_charts/deck10_pvalue.py` → `figures/10_0N_*.png`. 스크립트는 그림만 그린다. 모델 학습, 노트북 실행 없음.
E4의 "10,000번 모의"는 `numpy` 난수로 정규 표본 평균을 뽑는 것뿐이며(시드 고정, 초 단위) 학습이 아니다.

| 라벨 | 제작 | 파일 / 내용 | 슬라이드 |
| :-- | :-- | :-- | :-- |
| `fig:pv-coin` | 스크립트 | `10_01_coin_pvalue.png`. 2패널. 왼쪽: 동전 2번, 앞면 수 0/1/2의 확률 막대(0.25, 0.5, 0.25). 오른쪽: 동전 5번, 앞면 수 0~5의 경우의 수 막대(1, 5, 10, 10, 5, 1). 관측(4), 똑같이 드묾(1), 더 드묾(0, 5)을 세 색으로. 제목에 $12/32 = 0.375$ | 9, 10, 12 |
| `fig:pv-height` | 스크립트 | `10_02_height_pvalue.png`. $N(155.7, 6.89^2)$. 왼쪽: 142 아래, 169 위 꼬리 칠하기(각 0.025). 오른쪽: [155.4, 156] 띠와 양쪽 나머지(0.48, 0.48). $\sigma$ 가 책의 유도값임을 캡션에 | 13, 14 |
| `fig:pv-vitd` | 스크립트 | `10_03_vitd_null.png`. 귀무가설 아래 표본 평균 10,000개 히스토그램(평균 100, SE 3.3, 시드 고정) + 관측값 63 세로선. 두 번째 패널: 관측 평균 중심의 분포와 95% 구간 $[55.4, 68.6]$ | 15, 17, 18 |
| `fig:pv-ci-test` | 스크립트 | `10_04_ci_vs_test.png`. 위: 비타민 D 95%, 99% 구간과 귀무값 100. 아래: 배 99% 구간 $[-2.44, 10.44]$ 와 귀무값 0 | 19, 20 |
| (선택) `fig:pv-false-positive` | 스크립트 | `10_05_false_positive.png`. 약 A 대 약 A(두 집단 모두 완치율 37%, 각 198명) 실험 1,000번의 p값 히스토그램. 약 5%가 0.05 아래. 순수 난수 모의(학습 아님). 슬라이드 6 문장을 눈으로 보인다 | 5, 6 |
| `fig:pv-three-parts` | TikZ | 동전 2번 수형도(슬라이드 9 `image18` 의 구조)와 세 부분 상자(관측 / 똑같이 드묾 / 더 드묾). 값 없는 흐름도 | 9, 10 |

| 라벨 | 표 |
| :-- | :-- |
| `tab:pv-symbols` | 새 기호만(7.3.2 (1)) |
| `tab:pv-drug-trials` | 사례 · 약 · 완치 · 비완치 · 완치율(재계산) · p값(슬라이드) · 판정. 행: 1명씩, 2명씩, 대규모, 약 200명씩(R1~R3), A 대 A 세 쌍(R4, R5), 34% 대 35%(R6) |
| `tab:pv-error-types` | 진실(차이 없음/있음) × 판정(기각/기각 실패). 거짓 양성(1종 오류) 칸만 이 장에서 채우고 나머지 한 오류 칸은 "`sec:pv-future`" 로 표시 |
| `tab:pv-ci-vs-p` | 신뢰수준 · 유의수준 · 귀무값이 구간 밖이면 · 안이면 (95/0.05, 99/0.01) |

#### 7.3.7 `\Cref` 대상 (현재 .tex 에서 존재 확인함)

`lec09`: `ch:hypothesis`, `sec:ht-background`, `def:ht-null`, `def:ht-alt`, `sec:ht-decision`, `pit:ht-fail-not-proof`,
`tab:ht-wording`, `sec:ht-forms`, `fig:ht-tails`, `pit:ht-direction`, `sec:ht-pvalue`, `def:pvalue`, `pit:ht-alpha`,
`sec:ht-simulation`, `ex:ht-puppy`, `sec:ht-exact`, `ex:ht-juan`, `fig:ht-flow`, `sec:ht-future`, `tab:ht-symbols`.
`03`: `sec:ci`, `subsec:ci-compare`, `fig:ci-compare`, `pit:sd-se-ci`, `subsec:bootstrap`.
`04`: `sec:normal`(stub), `def:normal`, `sec:binomial`(stub). 기타: `ch:hw1`, `ch:project`, `tab:analogy`.
새 장 사이: `ch:compare-dist`(U8이 만든다. `lec10` 과 `lec11` 은 같은 빌드에 들어가야 `\Cref` 가 풀린다. pm의 순서 조건).

### 7.4 덱 11 → 새 장 `lec11-comparing-distributions.tex` 개요

- 파일 `chapters/lec11-comparing-distributions.tex`, 라벨 `ch:compare-dist`, 절 라벨 접두사 `sec:cd-`.
- 장 제목(줄표 없음): `분포 비교: 평균, 분산, 분포 전체를 검정으로 가르기`
- 출처: `notes/11_Comparing Distributions.pptx`(24 슬라이드). 외부 문헌 표기 없음. 그림 속 예제(Rory, 토마토, 만화 시청 시간,
  $N(0,1)$ 100개, $X$/$Y$ 두 표본)는 슬라이드를 출처로 적는다. t표, F표, KS 임계값표(`image20`, `image30`, `image45`)는
  그대로 옮기지 않고 필요한 한 칸만 인용한다.
- 비유: 지도 비유를 늘려 쓴다. KS는 "두 지도를 겹쳤을 때 가장 크게 어긋나는 한 지점의 거리"로 한 줄. 새 비유 집합 없음.

#### 7.4.1 장 머리 In brief

- **Why:** "두 집단이 다르다"는 말에는 세 가지 뜻(평균이 다르다, 퍼짐이 다르다, 모양 전체가 다르다)이 있고, 뜻마다 맞는 검정이 따로 있다.
- **When:** 두 표본(또는 셋 이상의 집단)을 놓고 "같은 분포에서 나왔는가"를 판정할 때. 데이터 분석에서 가장 흔한 질문이다(슬라이드 2).
- **Where:** 반바지 대 긴바지 달리기 기록, 두 밭의 토마토 키, 학년별 만화 시청 시간, 표본이 $N(0,1)$ 에서 왔는가, HW2의 모형 적합.
- **How:** 평균이면 t검정(t-test), 셋 이상 집단의 평균이면 분산분석(ANOVA)의 F검정(F-test), 분포 전체면 콜모고로프-스미르노프 검정(Kolmogorov-Smirnov Test) → 통계량을 계산한다 → 귀무가설 아래 그 통계량의 분포에서 p값을 읽는다(`\Cref{ch:pvalue}`).

#### 7.4.2 절 구성

**(1) 배경 `sec:cd-background`**

| 선행 개념 | 어디서 다뤘나 | 처리 |
| :-- | :-- | :-- |
| 가설검정 절차, 단측/양측 | `\Cref{fig:ht-flow}`, `\Cref{sec:ht-forms}`, `\Cref{fig:ht-tails}` | 한 줄 + 링크 |
| p값, 연속 데이터의 p값 = 꼬리 넓이, $z$ | `\Cref{def:pvalue}`, `\Cref{sec:pv-continuous}`, `\Cref{sec:pv-sim-vs-param}` | 한 줄 + 링크. t는 "표준오차를 표본에서 추정한 $z$" |
| 표준편차, 분산, 제곱거리 합 | `\Cref{sec:spread}`, `\Cref{def:spread}` | 한 줄. ANOVA의 SS가 이것 |
| 표준오차 | `\Cref{pit:sd-se-ci}` | 한 줄. $s/\sqrt n$ |
| 합친 평균 대 집단 평균 | `\Cref{sec:ht-two-groups}`, `\Cref{ex:ht-distance}` | 두 문장. "덱 9가 남긴 숙제를 이 장이 푼다" |
| CDF, 경험적 누적분포 | `\Cref{sec:cdf}`(stub) | 04가 stub이므로 경험적 CDF(Empirical CDF)는 이 장 `sec:cd-ks` 에서 직접 정의 |
| 자유도(Degrees of Freedom) | 없음 | 두 문장으로 직접 정의: "평균을 추정하는 데 값 하나를 쓰면 자유롭게 움직일 수 있는 값이 하나 줄어든다". $n - 1$ 의 이유 |

기호 표 `tab:cd-symbols`: $\bar x$, $s$(표본 표준편차), $\mu_0$, $t$, $\nu$ 또는 df(자유도), $m$(집단 수), $n$(집단당 크기),
SSB, SSW, $F$, $F_{\text{obs}}$, $F_{\text{exp}}$, $D_n$. **경고 하나:** 슬라이드는 $F$ 를 두 뜻으로 쓴다. F통계량(ANOVA)과
누적분포함수 $F_{\text{obs}}$, $F_{\text{exp}}$(KS). 첨자 유무로 구분하고 `pitfall` 로 밝힌다. 슬라이드의 $\sigma_x$ 는 모집단 값이
아니라 표본 표준편차이므로 본문은 $s$ 로 쓰고 이유를 한 줄 적는다.

**(2) 왜 필요한가 `sec:cd-why`** (슬라이드 2)

- 문제 1: `\Cref{ex:ht-distance}` 는 "제곱거리가 0.7%밖에 줄지 않았다"에서 멈췄다. 얼마나 줄어야 "크다"인지 기준이 없었다.
- 문제 2: `\Cref{subsec:ci-compare}` 의 구간 비교는 한 방향으로만 강하다("겹친다고 같은 것은 아니다"). 겹칠 때 판정할 도구가 없었다.
- 문제 3: HW2(`\Cref{sec:hw2-compare-how}`)는 $D$ 를 계산했지만 "얼마면 기각인가"를 정하지 않았다.
- 처방: 차이를 **표준오차 단위로 잰 통계량**을 만들고, 귀무가설 아래 그 통계량의 분포에서 p값을 읽는다. 무엇을 비교하느냐에 따라 통계량이 셋으로 갈린다.

**(3) 무엇인가: 세부 절**

| 절 | 라벨 | In brief 초안 (Why / When / Where / How) | 슬라이드 |
| :-- | :-- | :-- | :-- |
| 무엇을 비교할지 고르기 | `sec:cd-choose` | 개관 절, In brief 생략. 질문 세 개(치료 효과, 마라톤 나이, 암 유전자)와 세 갈래(평균/분산 또는 셋 이상 집단/분포 전체). `fig:cd-choose` | 2 (덱 10: 22, 23) |
| 단일 표본 t검정 | `sec:cd-t-one` | Why: 모집단 표준편차를 모르므로 $z$ 를 쓸 수 없다. / When: 평균 하나를 기준값 $\mu_0$ 와 비교할 때, 또는 한 집단을 두 조건에서 잰 차이(대응 설계, Within-subjects Design). / Where: 배터리 10시간, 시험 75점, 혈압 8 mmHg, 교사 경력 5년, 반바지 대 긴바지. / How: $t = (\bar x - \mu_0)/(s/\sqrt n)$, 자유도 $n-1$ 의 t분포에서 p값. | 3, 4, 5 |
| t표 읽기 | `sec:cd-t-table` | Why: 소프트웨어가 없을 때도 p값의 범위를 알아야 한다. / When: $t$ 와 자유도를 구한 뒤. / Where: $t = 2.75$, $n = 6$. / How: 자유도 행에서 $t$ 를 끼는 두 열을 찾고, 그 열의 꼬리 넓이를 읽는다. 양측이면 두 배. | 9 |
| 두 표본 t검정 | `sec:cd-t-two` | Why: 서로 다른 사람들로 이뤄진 두 집단(독립 설계, Between-subjects Design)은 차이를 사람별로 뺄 수 없다. / When: 두 독립 표본의 평균 비교. / Where: 남녀 급여, 과외 유무, 두 약, 두 매장, 두 밭의 토마토. / How: $t = (\bar x_1 - \bar x_2)/\sqrt{s_1^2/n_1 + s_2^2/n_2}$. | 6, 7, 8 (덱 10: 24, 25) |
| 분산분석과 F검정 | `sec:cd-anova` | Why: 집단이 셋 이상이면 t검정을 쌍마다 반복해야 하고, 검정을 여러 번 하면 거짓 양성이 쌓인다. / When: 셋 이상 집단의 **평균**이 모두 같은지 한 번에 물을 때. / Where: 대학생, 고등학생, 초등학생의 만화 시청 시간. / How: $F = \dfrac{\mathrm{SSB}/(m-1)}{\mathrm{SSW}/(m(n-1))}$, 크면 기각. F표에서 임계값을 읽는다. | 10, 11, 12 |
| KS 검정: 표본 대 가설 분포 | `sec:cd-ks` | Why: 평균과 분산이 같아도 모양이 다를 수 있다. / When: 표본이 특정 분포(예: $N(0,1)$)에서 왔는지 물을 때. / Where: 관측값 100개가 표준정규인가. / How: 데이터를 정렬 → 경험적 CDF $F_{\text{obs}}$ → 가설 CDF $F_{\text{exp}}$ → 세로 격차의 최댓값 $D_n$. | 13~20 |
| KS 검정: 두 표본 | `sec:cd-ks-two` | Why: 가설 분포가 없고 표본만 둘 있을 때도 모양을 비교해야 한다. / When: 두 표본이 같은 분포에서 왔는지. / Where: $X$ 10개 대 $Y$ 8개, HW2의 데이터 대 모형 표본. / How: 합친 값을 정렬하며 두 경험적 CDF를 같이 올리고, 가장 큰 격차를 $D$ 로. | 21, 22 |
| $D$ 에서 p값으로 | `sec:cd-ks-pvalue` | Why: $D$ 만으로는 "큰가"를 말할 수 없다(슬라이드 23). / When: $D$ 를 구한 뒤. / Where: 위 두 예제. / How: 임계값표(작은 $n$) 또는 큰 표본 근사 $1.36/\sqrt n$(한 표본, $\alpha = 0.05$). 실무는 `scipy.stats.kstest`, `ks_2samp`. | 23 |

정의 상자: `def:t-statistic`(단일, 두 표본을 한 상자에), `def:f-statistic`, `def:ecdf`, `def:ks-statistic`.
`def:ks-statistic` 이 KS의 **유일한** 정의 자리다(7.2 결정 5).

절 안의 `pitfall` (필수):
- `sec:cd-t-one`: 슬라이드 3 `image1` 의 "$t = \bar x / \bar\sigma$" 는 $\sqrt n$ 과 $\mu_0$ 가 빠진 식이다(R1). 올바른 식은 같은 슬라이드 `image11`.
- `sec:cd-t-two`: 자유도를 무엇으로 잡느냐에 따라 p값이 조금 달라진다(R6). 결론이 경계 근처면 방식을 밝힌다.
- `sec:cd-anova`: 슬라이드 10의 제목 "Comparing Variances"와 슬라이드 2의 "Variances → F-test"(R7). ANOVA가 검정하는 것은
  **평균**이 같은지이고, 분산은 그 판정에 쓰는 도구다. 두 분산이 같은지 묻는 F검정은 같은 F분포를 쓰는 **다른** 검정이다.
  이 장은 그 검정을 이름만 적고 계산하지 않는다(덱에 예제가 없다).
- `sec:cd-anova` 연결 `keyidea`: 집단이 둘이면 $F = t^2$(합동 분산 t검정). `\Cref{ex:ht-distance}` 에서 확인(E5).
- `sec:cd-ks`: 격차는 계단의 **양쪽**에서 재야 한다(R11). 슬라이드 19는 계단 윗점만 재어 0.092를 얻었지만 계단 바로 아래까지 재면 0.096이다.
- `sec:cd-ks`: 모수를 같은 표본에서 추정한 뒤 KS를 쓰면 p값이 낙관적이 된다. 04의 함정(`\Cref{sec:normal}`)을 한 줄로 가리키고,
  이 장의 예제는 $N(0,1)$ 을 **미리** 정했으므로 해당하지 않는다고 적는다. HW2는 모수를 데이터에서 추정했으므로 해당한다.
- `sec:cd-ks-two`: 슬라이드 21의 $F_y$ 행은 0.1 단위로 반올림돼 있다(R13). $Y$ 는 8개라 계단 높이가 $1/8$ 이다.

**(4) 어떻게 적용하는가 `sec:cd-apply`**

- 표 `tab:cd-tests`: 질문 · 비교 대상 · 통계량 · 귀무가설 아래 분포 · 이 장 예제 · p값. 한눈에 고르는 표(= `fig:cd-choose` 의 표 형태).
- 이 책 안의 적용 자리:
  - `\Cref{subsec:ci-compare}`: 구간이 겹칠 때 쓸 "별도의 검정"이 두 표본 t검정이다. 버거 대 치킨너겟은 새로 계산하지 않는다(치킨너겟 표준편차가 본문에 없다).
  - `\Cref{sec:hw2-compare-how}`, `\Cref{tab:hw2-airport-verdict}`, `\Cref{tab:hw2-movie-verdict}`: HW2의 $D$ 는 두 표본 KS 통계량이다.
    판정을 $D$ 크기 순위가 아니라 p값으로 바꾸면 무엇이 달라지는지 한 문단(값은 HW2 표의 것만 쓰고 새 계산을 하지 않는다. 필요하면 Q9).
  - `\Cref{sec:hw2-learned}`: "KS 거리 하나로는 왜 지는지 알 수 없다" = 슬라이드 23. QQ 도표와 함께 써야 하는 이유.
  - `\Cref{fig:violin}`: 보스턴 마라톤 연도별 분포. 슬라이드 2의 "여성 주자가 남성보다 젊은가"를 두 표본 t검정(평균)과 KS(모양)로 각각 물으면 다른 질문이 된다는 예.
  - `\Cref{ch:project}`: 비교 질문을 쓰기 전에 `fig:cd-choose` 로 검정을 고르는 점검.
- 확인 문제(슬라이드 24 Kahoot 대신) 3개: 질문에 맞는 검정 고르기, $n = 6$, $t = 2.75$ 의 양측 p 범위, $m = 3$, $n = 3$ 의 두 자유도.

**(5) 앞으로 `sec:cd-future`**

- 정규성 가정: t검정과 F검정은 정규분포를 가정한다(슬라이드 3 `image1` 의 "If X is normally distributed"). 가정을 확인하는
  Q-Q 플롯은 덱 7(`lec07`, 계획)의 몫이다. `lec07` 이 생기기 전까지는 `\Cref` 없이 문장으로만 적는다.
- 여러 번 검정: 암 유전자처럼 수천 번 검정하면 0.05 기준의 거짓 양성이 쌓인다(`\Cref{sec:pv-threshold}`). 덱에 해법이 없으므로 질문으로.
- 모수를 추정한 뒤의 KS(04 함정)와 정규성 전용 검정. 덱에 없으므로 질문으로.
- 순열검정(Permutation Test): 강의계획서가 예고한 주제(`lec09` `sec:ht-future`). 분포 가정 없이 두 표본을 비교하는 길로 적는다.

#### 7.4.3 슬라이드 → 절 대응 (모든 슬라이드)

| 슬라이드 | 내용 | 쓰는 절 | 비고 |
| :-- | :-- | :-- | :-- |
| 1 | 제목(t, F, KS) | (장 제목) | 덱 10 슬라이드 22와 같음 |
| 2 | 두 표본, 세 질문, 무엇을 비교할지 | `sec:cd-why`, `sec:cd-choose` | 덱 10 슬라이드 23과 같음. R7 |
| 3 | t통계량, 반바지 대 긴바지, 단일 표본 공식 | `sec:cd-t-one` | R1, R2. `image1`(식 오류), `image11`(올바른 식), `image2`($\sigma_{\bar X} = \sigma/\sqrt n$), `image8`, `image9` |
| 4 | 단일 표본 예시 셋(배터리, 시험, 혈압) | `sec:cd-t-one` | 계산 없음. 목록으로 |
| 5 | Rory, $n = 25$, $\bar x = 4$, $s = 2$, $H_a: \mu < 5$ | `sec:cd-t-one` | R3. 예제 E1 |
| 6 | 두 표본 t, 남녀 급여, 공식 | `sec:cd-t-two` | `image6` = 덱 10 `image49` |
| 7 | 두 표본 예시 넷 | `sec:cd-t-two` | 계산 없음 |
| 8 | 토마토 두 밭 | `sec:cd-t-two` | R5, R6. 예제 E3. 덱 10 슬라이드 25와 같음 |
| 9 | t표로 p값 찾기, $t = 2.75$, $n = 6$, p = 0.04 | `sec:cd-t-table` | R4. 예제 E2 |
| 10 | ANOVA, $F = \mathrm{VAR}_{\text{between}}/\mathrm{VAR}_{\text{within}}$ | `sec:cd-anova` | R7. `image24` |
| 11 | 만화 시청 시간 3×3, SSW = 6, SSB = 24, F = 12 | `sec:cd-anova` | R8~R10. 예제 E4 |
| 12 | 임계값 3.46, 12 > 3.46, 기각 | `sec:cd-anova` | R11 |
| 13 | KS 소개, $H_0$: 표본이 $P$ 에서 왔다 | `sec:cd-ks` | `image32` |
| 14 | KS 통계량 그림 | `sec:cd-ks` | `image33`. 슬라이드 20과 같은 그림. **한 번만** 그린다(`fig:cd-ks-one`) |
| 15 | 관측 CDF 대 기대 CDF 정의 | `sec:cd-ks` | R12. `def:ecdf` |
| 16 | $D_n$ 정의, 표본이 커지면 0으로 | `sec:cd-ks` | R14. `def:ks-statistic` |
| 17 | 100개 정렬, $F_{\text{obs}}(x_i) = i/100$ | `sec:cd-ks` | 예제 E6. `image39`(정렬), `image40`(원자료) |
| 18 | $F_{\text{exp}}(x_i) = \Phi(x_i)$ 표 | `sec:cd-ks` | `image41` |
| 19 | 절댓값 차 표, $D_n = 0.092$ | `sec:cd-ks` | **R11(한쪽 격차만)** |
| 20 | KS 통계량 그림(반복) | `sec:cd-ks` | 슬라이드 14와 같음. `\Cref{fig:cd-ks-one}` |
| 21 | 두 표본 $X$, $Y$, 합쳐 정렬한 표 | `sec:cd-ks-two` | R13, R15. 예제 E7. 제목 "Comparing to Samples"는 "two"의 오타 |
| 22 | 두 표본 계단 그림, $D_n = 0.6$ | `sec:cd-ks-two` | `image44`(계단 높이가 1/8로 맞게 그려져 있다) |
| 23 | $D$ 의 분포, 임계값표 | `sec:cd-ks-pvalue` | R16. `image45` |
| 24 | Kahoot | `sec:cd-apply` 확인 문제 | 내용 없음 |

#### 7.4.4 재계산 표 (전사 금지)

| # | 슬라이드 | 슬라이드 값 | 재계산 | 판정 |
| :-- | :-- | :-- | :-- | :-- |
| R1 | 3 `image1` | $t = \bar x / \bar\sigma$ | 단일 표본 t는 $(\bar x - \mu_0)/(s/\sqrt n)$. 대응 설계에서 차이의 기준값이 0이면 $\mu_0 = 0$ 이라 분자는 맞지만 **분모의 $\sqrt n$ 이 빠졌다** | **오류.** 같은 슬라이드 `image11` 과 슬라이드 5 `image5` 는 올바른 식 |
| R2 | 3 `image11`, 5 `image14` | 분모 $\sigma_x/\sqrt n$ | 표본 표준편차이므로 $s$ 가 맞는 기호. 슬라이드 5 `image5` 는 $s_x$ 를 쓴다 | 기호 혼용. 본문은 $s$ 로 통일 |
| R3 | 5 | $n = 25$, $\bar x = 4$, $s = 2$, $H_a: \mu < 5$. 결과 없음 | $s/\sqrt n = 2/5 = 0.4$. $t = (4-5)/0.4 = -2.5$. 자유도 24. 단측 p = $P(t_{24} < -2.5) \approx 0.0098$($t_{0.01,24} = 2.492$ 바로 위). `scipy.stats.t.cdf(-2.5, 24)` 로 확인 | 슬라이드는 결론을 주지 않는다. $\alpha = 0.05$ 로 기각. 본문 결론 문장은 `\Cref{tab:ht-wording}` 형식 |
| R4 | 9 | $t = 2.75$, $n = 6$, 자유도 $n-1 = 5$, p = 0.04 | 자유도 5 행에서 2.75는 $t_{0.02} = 2.757$ 바로 아래 → 한쪽 꼬리 약 0.020, **양측 약 0.040** | 맞음(양측). 본문은 "양측이라 두 배"를 명시. `image20`(전체 t표)에는 0.02 열이 없고 `image21`, `image22` 는 다른 표의 한 줄이다. 본문은 표를 옮기지 않고 한 칸만 인용 |
| R5 | 8 (= 덱 10 슬라이드 25) | $\bar x_A = 1.3$, $s_A = 0.5$, $n_A = 22$. $\bar x_B = 1.6$, $s_B = 0.3$, $n_B = 24$. $t \approx -2.44$ | $0.25/22 = 0.011364$, $0.09/24 = 0.00375$, 합 $0.015114$, 제곱근 $0.12294$. $t = -0.3/0.12294 = -2.440$ | 맞음 |
| R6 | 8 | $P(\lvert t \rvert \ge 2.44) \approx 0.024$ | 자유도를 $\min(n_A, n_B) - 1 = 21$ 로 잡으면 양측 p ≈ 0.024. Welch 자유도 $\approx 33.8$ 이면 p ≈ 0.020. `scipy.stats.ttest_ind_from_stats(1.3,0.5,22,1.6,0.3,24,equal_var=False)` 로 확인 | 맞음(보수적 자유도). **플래그(관례):** 자유도를 밝히지 않았다. 어느 쪽이든 0.05로 기각 |
| R7 | 2, 10 | "Variances (or more than two categories) → F-test", 제목 "Comparing Variances" | ANOVA의 귀무가설은 "모든 집단의 **평균**이 같다". 분산 비율은 그것을 판정하는 통계량이다 | **플래그(개념).** 7.4.2 함정. 슬라이드 11~12의 예제도 평균 비교다(결론 "교육 수준이 중요하다") |
| R8 | 11 | 3집단 {3, 2, 1}, {5, 3, 4}, {5, 6, 7}, 전체 평균 = 4 | 합 $6 + 12 + 18 = 36$, $36/9 = 4$. 집단 평균 2, 4, 6 | 맞음 |
| R9 | 11 | SSW = 6, SSB = 24, 자유도 6과 2, $F = (24/2)/(6/6) = 12$ | SSW: $(1+0+1) + (1+1+0) + (1+0+1) = 6$. SSB: $3[(2-4)^2 + (4-4)^2 + (6-4)^2] = 3 \times 8 = 24$. 검산 SST = $\sum (x - 4)^2 = 1+4+9+1+1+0+1+4+9 = 30 = 24 + 6$. 자유도 $m - 1 = 2$, $m(n-1) = 6$, 전체 $mn - 1 = 8 = 2 + 6$. $F = 12/1 = 12$ | 맞음. **플래그(라벨):** SSW 줄 옆의 "m*n − 1"은 전체 자유도(8)이지 SSW의 자유도(6)가 아니다. 본문은 분산분석표 `tab:cd-anova` 로 세 줄을 분리 |
| R10 | 11 `image31` | "significance level $\alpha < 0.10$" | 유의수준은 부등식이 아니라 값: $\alpha = 0.10$ | 표기 오류. 슬라이드 12는 $\alpha = 0.10$ 으로 맞게 쓴다 |
| R11 | 12 `image30` | 임계값 $F_{0.10}(2, 6) = 3.46$ | F표($\alpha = 0.10$) 분자 2, 분모 6 칸: 3.46330 | 맞음. 추가: 분자 자유도 2의 꼬리는 닫힌 식 $P(F > f) = (1 + 2f/\nu_2)^{-\nu_2/2}$. $(1 + 24/6)^{-3} = 5^{-3} = 0.008$. p = 0.008로 1% 기준으로도 기각 |
| R12 | 15 `image34` | "$F_{\text{obs}}(x)$ = #observations **below** $x$ / #observations", $F_{\text{obs}}(y_i) = i/n$ | $i/n$ 이 되려면 "$x$ **이하**(at or below)" 여야 한다 | **플래그(정의).** `def:ecdf` 는 "이하"로 쓴다 |
| R13 | 21 | $F_y$: 0.1, 0.2, 0.4, 0.5, 0.6, 0.8, 0.9, 1.0 | $Y$ 는 8개이므로 계단 높이 $1/8$: 5.6에서 0.125, 6.5에서 0.25, 6.6에서 0.375, 6.9에서 0.5, 9.2에서 0.625, 10.4에서 0.75, 10.6에서 0.875, 19.3에서 1 | **오류(반올림).** `image44` 그림은 1/8 계단으로 맞다. $D$: 4.8에서 $F_x = 0.6$, $F_y = 0$ → **0.6**. 다른 후보(5.6에서 $0.6 - 0.125 = 0.475$, 17.3에서 $\lvert 0.8 - 0.875 \rvert = 0.075$)보다 크다. **$D = 0.6$ 은 맞다** |
| R14 | 16 | 표본이 가설 분포에서 왔으면 $n \to \infty$ 일 때 $D_n \to 0$ | 글리벤코-칸텔리 정리(Glivenko-Cantelli Theorem)의 내용 | 맞음. 정리 이름은 `aside` 에 한 줄(슬라이드는 이름을 쓰지 않는다. 문헌 항목은 만들지 않는다) |
| R15 | 17~19 | 100개, $D_n = 0.092$(88번째, $x = 0.80$) | 88번째: $F_{\text{obs}} = 0.88$, $\Phi(0.80) = 0.7881$, 차 0.092. 그러나 $D_n = \sup_x \lvert F_{\text{obs}} - F_{\text{exp}} \rvert$ 는 계단 바로 **아래**도 봐야 한다: $\max_i \{ i/n - F(x_i),\ F(x_i) - (i-1)/n \}$. 27번째 $x = -0.37$ 에서 $F(x) = 0.356$, 바로 아래 $F_{\text{obs}} = 26/100$, 격차 **0.096** | **플래그(계산 방식).** 슬라이드는 $i/n - F(x_i)$ 쪽만 계산했다. 정확한 $D_n \approx 0.096$(표의 반올림 값 기준). `scipy.stats.kstest(data, 'norm')` 으로 확인(자료 100개는 `image40` 에서 옮겨 적고, 정렬 결과가 `image39` 와 같은지로 전사를 검증). 결론은 같다: 임계값 $1.36/\sqrt{100} = 0.136$ 보다 작으므로 기각 실패, p는 약 0.3(점근식). 슬라이드는 결론을 쓰지 않는다 |
| R16 | 23 `image45` | 임계값표($n = 1$~$6$) | 이 표는 **한 표본** KS용이고 $n \le 6$ 만 보인다. 예제 E6($n = 100$)과 E7(두 표본 10, 8)에는 직접 못 쓴다 | **플래그(적용 범위).** 한 표본 큰 $n$: $1.36/\sqrt n$. 두 표본: 점근 임계값 $1.36\sqrt{(n+m)/(nm)} = 1.36\sqrt{18/80} = 0.645$ → $D = 0.6 < 0.645$, 점근 p 약 0.08(기각 실패). **그러나 정확값은 경계다:** $D \ge 0.6$ 은 격자 위에서 $\lvert 4i - 5j \rvert \ge 24$ 와 같고, 이를 만나는 경로를 세면 $2182 / \binom{18}{8} = 2182/43758 = 0.0499$ 로 $\alpha = 0.05$ 에서 **기각**이다(경로 수는 손으로 센 값이라 확인 필요). **플래그(판정 뒤집힘):** 점근식과 정확값이 판정을 가른다. 본문 숫자와 결론은 `scipy.stats.ks_2samp(X, Y, method='exact')` 값으로 정하고, 그 전까지 sonnet-writer는 "기각 실패"를 쓰지 않는다. `sec:cd-ks-pvalue` 의 함정 재료 |
| R17 | (책 자체, E5) | 없음 | `\Cref{ex:ht-distance}` 값으로 ANOVA: SSB = 1.50(자유도 1), SSW = 219.33(자유도 $2 \times 2 = 4$). $F = 1.50 / (219.33/4) = 1.50/54.83 = 0.0274$. 합동 t: $\bar x_D - \bar x_C = 1$, $\mathrm{SE} = \sqrt{54.83 \times (1/3 + 1/3)} = 6.046$, $t = 0.165$, $t^2 = 0.0274 = F$. p ≈ 0.88 | 덱 9의 "기각 실패"가 수치로 확인된다. 덱 9 `sec:ht-future` 의 열린 질문 (a)의 답 |

#### 7.4.5 예제

- **E1 `ex:cd-rory`: 교사 경력(슬라이드 5).** 단측, $t = -2.5$, 자유도 24, p ≈ 0.0098 → 기각. 한 줄 한 계산.
- **E2 `ex:cd-t-table`: t표 읽기(슬라이드 9).** $t = 2.75$, 자유도 5, 양측 0.04.
- **E3 `ex:cd-tomato`: 두 밭의 토마토(슬라이드 8, 덱 10 슬라이드 25).** $t = -2.44$, p ≈ 0.024(자유도 21) / 0.020(Welch). 표 `tab:cd-tomato`.
- **E4 `ex:cd-cartoon`: 만화 시청 시간 ANOVA(슬라이드 11, 12).** 분산분석표 `tab:cd-anova`(요인 · SS · 자유도 · 평균제곱 · F). F = 12, 임계값 3.46, p = 0.008.
- **E5 `ex:cd-revisit`: 약 C 대 D 다시 보기(책 자체, R17).** 덱 9의 예제를 F와 t로 닫는다. $F = t^2$ 확인.
- **E6 `ex:cd-ks-normal`: 100개가 $N(0,1)$ 인가(슬라이드 17~19).** 표 전체를 옮기지 않고 행 몇 개만(27번째, 88번째 포함). R15의 양쪽 격차.
- **E7 `ex:cd-ks-two`: $X$ 대 $Y$(슬라이드 21, 22).** 합친 정렬 표를 **1/8 계단으로 고친** `tab:cd-ks-two` 로. $D = 0.6$.
  p값은 R16: 점근 약 0.08(기각 실패) 대 정확 약 0.050(경계, 기각 쪽). 판정은 U8a의 `ks_2samp(method='exact')` 값으로 정하고,
  두 값을 나란히 둔 `pitfall` "작은 표본에서는 점근식이 판정을 뒤집을 수 있다"를 `sec:cd-ks-pvalue` 에 둔다.

#### 7.4.6 그림과 표

새 스크립트 `hw1/00_lecture_charts/deck11_compare.py` → `figures/11_0N_*.png`. 그림만 그린다. 학습, 노트북 실행 없음.
E6의 100개 값은 공식이 아니라 자료이므로 `image40` 에서 **한 번만** 옮겨 적고 출처 주석을 단다(R15의 전사 검증 포함).

| 라벨 | 제작 | 파일 / 내용 | 슬라이드 |
| :-- | :-- | :-- | :-- |
| `fig:cd-choose` | TikZ | 질문 → 평균(두 집단: t / 셋 이상: ANOVA F) / 분포 전체: KS(가설 분포 있음: 한 표본 / 없음: 두 표본). `fig:ht-flow` 와 같은 상자 양식 | 2 |
| `fig:cd-t-dist` | 스크립트 | `11_01_t_vs_normal.png`. $N(0,1)$ 과 t(자유도 5, 24) 겹치기. 자유도 5 곡선에 $\pm 2.75$ 양쪽 꼬리 칠하기(E2). "꼬리가 두껍다 = 같은 $t$ 에서 p가 더 크다" | 3, 9 |
| `fig:cd-anova` | 스크립트 | `11_02_anova_cartoon.png`. 3집단 점그림, 집단 평균선 2, 4, 6과 전체 평균선 4. 왼쪽 패널 SSW(점 → 집단 평균), 오른쪽 SSB(집단 평균 → 전체 평균). `fig:ht-pooled-vs-group` 과 같은 양식으로 맞춘다 | 11 |
| `fig:cd-ks-one` | 스크립트 | `11_03_ks_one_sample.png`. 100개의 경험적 CDF 계단과 $\Phi$. 0.092(88번째, 계단 위)와 0.096(27번째, 계단 아래) 두 격차를 표시. 슬라이드 14, 20의 `image33` 을 대신하는 **단일** 그림 | 14, 17~20 |
| `fig:cd-ks-two` | 스크립트 | `11_04_ks_two_sample.png`. $X$(1/10 계단), $Y$(1/8 계단), 4.8에서 $D = 0.6$ 세로선 | 21, 22 |

| 라벨 | 표 |
| :-- | :-- |
| `tab:cd-symbols` | 기호 표(7.4.2 (1)) |
| `tab:cd-tomato` | 밭 · 평균 · 표준편차 · $n$ · $s^2/n$ |
| `tab:cd-anova` | 분산분석표: 집단 간(24, 2, 12) · 집단 내(6, 6, 1) · 전체(30, 8). $F = 12$ |
| `tab:cd-ks-two` | 합친 값 18개 · $F_x$ · $F_y$(1/8 단위) · $\lvert F_x - F_y \rvert$. 최댓값 행 강조 |
| `tab:cd-tests` | 7.4.2 (4)의 검정 고르기 표 |

#### 7.4.7 `\Cref` 대상 (현재 .tex 에서 존재 확인함)

`lec09`: `ch:hypothesis`, `sec:ht-forms`, `fig:ht-tails`, `def:pvalue`, `tab:ht-wording`, `sec:ht-two-groups`, `ex:ht-distance`,
`fig:ht-pooled-vs-group`, `fig:ht-flow`, `sec:ht-future`. `02`: `sec:spread`, `def:spread`. `03`: `subsec:ci-compare`, `pit:sd-se-ci`,
`fig:violin`. `04`: `ch:distributions`, `sec:normal`(stub, 함정 포함), `sec:cdf`(stub). `hw02`: `ch:hw2`, `sec:hw2-compare-how`,
`tab:hw2-airport-verdict`, `tab:hw2-movie-verdict`, `sec:hw2-learned`. 기타: `ch:project`.
`lec10` 이 만들 라벨: `ch:pvalue`, `sec:pv-continuous`, `sec:pv-sim-vs-param`, `sec:pv-threshold`(U7). **존재하지 않아 쓰지 않는 것:**
`ch:qq-normalization`(lec07 계획 상태). 04 `sec:normal` 의 함정에는 라벨이 없다(`sec:normal` 로 가리킨다).

### 7.5 소유자 질문 (Q7~)

- **Q7 (KS 정의 자리):** KS 통계량의 정의와 검정을 `lec11` `sec:cd-ks` 에 두고, 04 `sec:cdf` 는 그림 + 한 줄 + `\Cref` 만 둔다
  (7.2 결정 5, 부록 "CDF 경계"의 KS 부분을 대체). 승인하는가? 반대하면 `lec11` 이 04를 가리키고 04 채우기(U2)가 `lec11` 보다 먼저여야 한다.
- **Q8 (검증된 장에 앞방향 링크 추가):** 이미 리뷰를 마친 장 세 곳에 한 줄씩 `\Cref` 를 넣어도 되는가?
  (a) `lec09` `sec:ht-future` 의 "정식 검정 통계량" 항목 → `\Cref{sec:cd-anova}`, (b) `03` `subsec:ci-compare` 함정의
  "별도의 검정" → `\Cref{sec:cd-t-two}`, (c) `hw02` `sec:hw2-compare-how` 의 $D$ 설명 → `\Cref{sec:cd-ks-two}`.
  권고: 예. 기존 문장은 고치지 않고 괄호 안 링크만 덧붙인다(latex.md §13). 아니오면 링크는 새 장 쪽에서만 건다.
- **Q9 (HW2 p값):** `lec11` 적용 절에서 HW2의 $D$ 값을 p값으로 바꿔 보여 줄 것인가? 그러려면 HW2 스크립트에서 `kstest` /
  `ks_2samp` 를 다시 불러야 한다(학습 아님, 초 단위). 권고: 이번에는 하지 않는다(새 숫자 없이 문장으로만 연결). 과제 제출물과 숫자가 갈리는 것을 피한다.
- **Q10 (슬라이드 오류 공개):** Q4의 방침(드러낸다, `pitfall`)을 덱 10, 11에도 그대로 적용하는가? 특히 (i) 덱 10 슬라이드 4의
  29%와 0.001%, (ii) 슬라이드 13의 거꾸로 된 귀무가설과 p = 0.05 경계 판정, (iii) 덱 11 슬라이드 19의 $D_n$ 0.092 대 0.096,
  (iv) 슬라이드 21의 $F_y$ 반올림. 퀴즈나 시험이 슬라이드 값을 정답으로 쓸 수 있으므로 "슬라이드 값은 X, 다시 계산하면 Y"로 둘 다 적는 것을 권고한다.
- **Q11 (자유도 관례):** 두 표본 t검정의 자유도를 본문에서 무엇으로 가르치는가? 슬라이드 8의 0.024는 보수적 자유도
  ($\min(n_1, n_2) - 1$)에서 나온다. 소프트웨어 기본값(Welch)은 0.020을 준다. 권고: 공식은 슬라이드 그대로, p값은 둘 다 적고 "소프트웨어는 Welch"라고 한 줄.

### 7.6 pm에게 넘길 설계 단위 (U7~)

순서와 배정은 pm이 정한다. 크기와 선후만 적는다.

| 단위 | 산출물 | 선행 | 크기 |
| :-- | :-- | :-- | :-- |
| U7a | `hw1/00_lecture_charts/deck10_pvalue.py` → `figures/10_01~10_05_*.png`(7.3.6). R3a의 `fisher_exact` 확인 값을 스크립트 출력에 찍어 U7에 넘긴다. `developer` | 없음 | 소 |
| U7 | `lec10-p-value.tex` + `part1` 에 `\input` 한 줄(`lec09` 다음). `sonnet-writer`. **`ch:compare-dist` 로의 앞방향 링크는 넣지 않는다**(아직 없는 라벨). "앞으로" 절은 문장으로만 | U7a, Q10 | 대 |
| U8a | `hw1/00_lecture_charts/deck11_compare.py` → `figures/11_01~11_04_*.png`(7.4.6). 100개 자료 전사와 검증(정렬 = `image39`), R3, R6, R15, R16의 `scipy` 확인 값 출력. `developer` | 없음 | 중 |
| U8 | `lec11-comparing-distributions.tex` + `part1` 에 `\input` 한 줄(`lec10` 다음) + `lec10` `sec:pv-future` 에 `\Cref{ch:compare-dist}` 한 줄 추가. `sonnet-writer` | U7, U8a, Q7, Q10, Q11 | 대 |
| U9 | Q8의 앞방향 링크 세 줄(`lec09`, `03`, `hw02`) | U8, Q8 | 소 |
| U2 (범위 변경) | 04 `sec:cdf` 는 KS를 정의하지 않고 `\Cref{sec:cd-ks}` 로 넘긴다(7.2 결정 5). U8 전에 U2를 하면 KS 문장만 쓰고 `\Cref` 는 U8 때 덧붙인다(미해결 참조 방지) | Q7, (`\Cref` 는 U8) | 줄어듦 |

U7, U8 공통 수용 기준 후보: 7.3.4 / 7.4.4의 재계산 값과 본문 숫자 일치(오류 행은 Q10 방침대로 두 값 병기), 슬라이드 대응표의
모든 슬라이드가 본문의 `% source: deck NN slide N` 주석으로 추적됨, p값 재정의 없음(`def:pvalue` 만, `grep` 으로 `\begin{definition}[p값]` 1건),
KS 정의 1건(`def:ks-statistic`), 줄표 0개, 모든 개념 절에 `inbrief`, `latexmk -xelatex main.tex` 에서 미해결 `\Cref` 0건,
모델 학습이나 노트북 실행 없음(`scipy` 확인 호출과 난수 모의는 허용 범위).

### 7.x 결정 기록: 비타민 D 관측 평균 (2026-09-23, 메인 세션, T19r I3)

R15를 따른다. 책 전체(본문·그림·신뢰구간)는 관측 평균 **63** 하나로 통일한다.
신뢰구간은 $63 \pm 2 \times 3.3 = [56.4,\ 69.6]$, 1.96 기준 $[56.53,\ 69.47]$ 로 다시 계산한다.
R13의 슬라이드 구간 $[55.4,\ 68.6]$(62 기준)은 pitfall 상자에서 "슬라이드 값은 X, 다시 계산하면 Y"로만 보인다.
§7.3.6과 T19 AC의 62 기준 구간은 이 결정으로 대체된다.

### 7.y 결정 기록: 두 표본 KS 점근 p값 (2026-09-23, 메인 세션, T22r I3)

슬라이드의 임계값 $0.645 = 1.36\sqrt{(10+8)/(10 \cdot 8)}$ 은 콜모고로프 극한분포에서 나온다. 책은 이 근사 하나로 통일한다.
점근 p값은 `kstwobign.sf(0.6 * sqrt(80/18))` $= 0.0815$ (기각 실패, $D = 0.6 < 0.645$).
정확 p값은 `ks_2samp(X, Y, method='exact')` $= 0.0499$ (기각). 이 대비가 E7의 요점이다.
scipy의 `method='asymp'` 값 $0.0674$(`kstwo.sf(0.6, 4)`, 유효 표본 크기를 4로 반올림)는 "또 다른 근사도 같은 판정"이라는 한 문장으로만 언급한다.
§7.4.4 R16의 "약 0.08"은 0.0815로 확정한다.
