# T22r review: `hw1/00_lecture_charts/deck11_compare.py` (cycle 1/2)

Date: 2026-09-23. Reviewer run: `uv run python hw1/00_lecture_charts/deck11_compare.py`, run once
(1.3 s wall, exit 0). Spec: PLAN.md T22/T22r, architecture.md §7.4.4 and §7.4.6.
Slide source: `notes_text/11_Comparing_Distributions/slides.md` + media image39, image40
(both zoomed 2-3x with PIL into the session scratchpad, not into `figures/`), image42, image43
(slide 21 table), image44.

## Verdict: ISSUE

Every printed number is correct and the four figures are right. **The raw data (`KS_DATA`,
image40) is correct.** The blocking issue is I1. The developer misread the *check* array
(`KS_DATA_SORTED_SLIDE`, image39) and not the raw data. So the script prints a
2-mismatch WARNING, and its docstring says the two slide images disagree when they do not.
The T22 AC "mismatch count must be 0" fails. Fixing it is one row of the check array plus
the docstring. No downstream number changes.

## (1) The −0.23 vs −0.17 question

**image40 (−0.23 twice) is right. image39 agrees with it. The developer's transcription of
image39 is wrong.**

- image40 row 7 col 10 = **−0.23** and row 10 col 1 = **−0.23**. The zoom is unambiguous.
  image40 has −0.17 three times: row 3 col 1, row 3 col 10, row 8 col 3.
- image39 sorted row 4, read at 3x zoom: `-0.32 -0.26 -0.26 -0.25 -0.24 -0.23 -0.23 -0.19 -0.19 -0.17`.
  Row 5 starts `-0.17 -0.17 -0.16`. That is **two −0.23 and three −0.17**, the same as image40.
- The script has row 4 as `... -0.24, -0.23, -0.19, -0.19, -0.17, -0.17` (l.41). It dropped
  one −0.23 and added one −0.17. That row is 10 wide only because it was shifted by one place.
- I re-checked with row 4 replaced by my reading. `np.sort(KS_DATA)` then has **0 position
  mismatches** against image39 over all 100 values. image39 rows 1-3 and 6-10 and image40
  rows 1-10 were all compared against the script at zoom and agree.
- D/p effect: none. The hypothetical reading (row 7 col 10 = −0.17) gives `kstest`
  D = 0.095691, p = 0.299681, identical to the raw reading. Both are the ±0.2 region, far from
  the 27th/88th rows. The point is moot anyway, because the raw reading is the right one.

## Mechanical ACs (as T02, plus T22)

| Check | Result | Evidence |
| :-- | :-- | :-- |
| Runs in seconds, exit 0 | PASS | 1.3 s wall, exit 0 |
| Four PNGs 11_01..11_04 exist | PASS | all four rewritten 19:56 (75555, 52498, 56792, 32935 bytes) |
| No training, no notebook, no RNG | PASS | exact arithmetic and `scipy.stats` sf/cdf/ppf/kstest/ks_2samp only. No RNG, so no seed is needed |
| 100 values transcribed once from image40, with a source comment | PASS | `KS_DATA` l.20-33, comment `media/image40.png (slide 17)`. All 100 match image40 |
| Sorted list checked vs image39, mismatch count 0 | **ISSUE (I1)** | the script prints `position mismatches = 2` + WARNING. The cause is the check array (image39 misread), not the data |
| Slide 21 X (10) and Y (8) transcribed once | PASS | `KS_X` = 1.2, 1.4, 1.9, 3.7, 4.4, 4.8, 9.7, 17.3, 21.1, 28.4. `KS_Y` = 5.6, 6.5, 6.6, 6.9, 9.2, 10.4, 10.6, 19.3. Both match image43 |
| `main.py` append-only | PASS (note) | numstat 7/1: +1 import, +3-line build block, and the total line edited to add `len(deck11)` (split over two lines). This is the same pattern T19r accepted |
| `uv run ruff check hw1/00_lecture_charts` | PASS | "All checks passed!" |
| Module size | note | 314 lines: over the 300 soft limit, under 400. One responsibility |

## (3) Printed values vs §7.4.4 / dispatch list

| Row | Printed | Expected | Result |
| :-- | :-- | :-- | :-- |
| R3 | `t.cdf(-2.5,24)` = 0.0098 | 0.0098 | PASS |
| R4 | `2*t.sf(2.75,5)` = 0.0403 | 0.040 | PASS |
| R5 | t = −2.4403 (SE 0.12294) | −2.440 | PASS |
| R6 | `2*t.sf(2.44,21)` = 0.0236. Welch p = 0.0201, df = 33.79 | 0.024. 0.020, 33.8 | PASS. The Welch df is computed by hand (Satterthwaite), which is correct |
| R9 | SSW 6, SSB 24, F 12 | same | PASS |
| R11 | `f.sf(12,2,6)` = 0.0080, `f.ppf(0.90,2,6)` = 3.4633 | 0.008, 3.46 | PASS |
| R15 | one-sided max 0.0919 at row 88 (x = 0.80). Below-step max 0.0957 at row 27 (x = −0.37). `kstest` D = 0.0957, p = 0.2997 | 0.092 / 88. 0.096 / 27 | PASS. `kstest` reports `statistic_location = −0.37`. D < 0.136 = 1.36/√100 → fail to reject |
| R16 | exact D = 0.6, p = 0.049865. Asymp p = 0.067400 | 0.6. 2182/43758 = 0.049865 | PASS for exact: the hand count is confirmed to 6 d.p. For asymp see I3 |
| R17 | SSB 1.50, SSW 219.33, F 0.0274. SE 6.046, t 0.1654, p 0.8767. t² = 0.0274 = F | same | PASS |

**Exact two-sample p (T22r's required statement): p = 0.049865 < 0.05. E7's verdict is 기각
(reject) at α = 0.05.** It is a boundary case, 0.00013 under the threshold.

## (4) Figures vs slides

| Figure | Result | Notes |
| :-- | :-- | :-- |
| 11_01 t vs normal | PASS | N(0,1), t24 and t5 are overlaid. The t5 tails beyond ±2.75 are shaded and labelled "two-sided p = 0.040" |
| 11_02 ANOVA | PASS (note) | Group means 2/4/6 and grand mean 4 are correct. The titles say SSW 6 (df 6) and SSB 24 (df 2), and the suptitle "F = 12, critical 3.46 → reject" is correct at α = 0.10. Minor: the right panel draws no group-mean markers, so the SSB segments end in empty space. Readable, not wrong |
| 11_03 KS one-sample | PASS (note, I2) | The ECDF and Φ are correct. The two gaps are marked at the 27th (0.096, below step, "true D") and the 88th (0.092, above step, "slide's calc"), which matches §7.4.6 and image42's 0.092. Minor: the "88th: gap 0.092" label overlaps the ECDF line |
| 11_04 KS two-sample | PASS for spec | X in 1/10 steps and Y in 1/8 steps (0.125 ... 1.0). The D = 0.6 line at x = 4.8 goes from F_y = 0 to F_x = 0.6. This matches image44 (whose steps are also 1/8), and it corrects image43's 0.1-step F_y (R13) |
| Exact-vs-asymptotic flip visible in figures | **ISSUE (I4)** | No figure shows it. 11_04 carries no p-value, no verdict and no 0.645 critical line. The flip currently exists only in stdout. §7.4.6 and the T22 AC never asked for it (they give it to T23's `sec:cd-ks-pvalue` pitfall). So this is a spec gap, not a developer defect. Nothing in the figures is *incorrect* about it |

## ISSUEs

| ID | Class | Route | Blocking | Detail |
| :-- | :-- | :-- | :-- | :-- |
| I1 | implementation | developer | yes (the AC names mismatch = 0) | `KS_DATA_SORTED_SLIDE` row 4 (l.41) must read `-0.32, -0.26, -0.26, -0.25, -0.24, -0.23, -0.23, -0.19, -0.19, -0.17`. Then remove the docstring claim in `_check_ks_transcription` (l.59-68) that image39 and image40 disagree, because they agree. That docstring's "checked at build time" claim also has no matching code. After the fix the check prints 0 mismatches and no WARNING. `KS_DATA` is correct and must NOT be changed |
| I2 | implementation | developer | no | 11_03: move the "88th" label clear of the ECDF line (for example, to the right or below) |
| I3 | reasoning | data-analyst / system-architect | not for T22. It must be settled before T23 writes KS numbers | **What "asymptotic p" means.** scipy 1.18.1 `ks_2samp(method='asymp')` is NOT the Kolmogorov limit. It returns `kstwo.sf(D, round(nm/(n+m)))` = `kstwo.sf(0.6, 4)` = **0.0674**. The classical limiting formula `kstwobign.sf(√(nm/(n+m))·D)` = `kstwobign.sf(1.265)` = **0.0815**. That is architecture's "≈0.08". It is also the formula behind the 0.645 critical value (1.36·√(18/80)) that §7.4.4 R16 puts in the text. The dispatch says "the book must use 0.067". But if the chapter prints 0.645 next to 0.067, the two numbers come from different approximations. Pick one pairing and name it: (a) 0.645 with 0.0815 ("Kolmogorov asymptotic"), or (b) 0.067 labelled "scipy asymp (kstwo, n_eff = 4)". Either way the verdict flip holds: both are > 0.05 and exact 0.0499 < 0.05. For the one-sample test (R15), `kstest` p = 0.2997 is the *exact* kstwo(100) value. The Kolmogorov limit gives 0.3191. §7.4.4's "약 0.3 (점근식)" fits both, but the text should not call 0.2997 "asymptotic" |
| I4 | reasoning (spec) | system-architect / pm | no | Check (4) asks for the exact-vs-asymptotic flip to be visible in a figure. §7.4.6 never specifies that, and 11_04 does not show it. If the owner wants it, add to the 11_04 spec a 0.645 critical-value line (or its p-value text box) next to the exact p 0.0499. Otherwise the T23 pitfall carries it alone |

**Gate:** cycle 2 goes to the developer for I1 (and optionally I2) only. I3 and I4 go to the architect/pm in parallel and do not hold T22.

No ladder/schema (L1-L4) checks apply: this is a book-figure script, not system/app work.
