# T02r: verification of T02 (deck9_hypothesis.py figures)

- Reviewer: reviewer agent, cycle 1/2, 2026-09-23
- Target: `hw1/00_lecture_charts/deck9_hypothesis.py`, `hw1/00_lecture_charts/main.py`, `figures/09_01..09_04`
- Spec: `research/PLAN.md` T02 AC; `research/architecture.md` §3.4 (R1-R13), §3.5 (E3), §3.6 (figure table)
- Slide source: `notes_text/9_Hypothesis_Testing/slides.md` + `media/`
- No training, no notebook run. Script is plotting + exact arithmetic only.

## Run

`uv run python hw1/00_lecture_charts/deck9_hypothesis.py`: exit 0, 1.4 s wall. Output:

```
Deck 9 - Hypothesis Testing
  09_02 pooled sum sq = 220.83, group sum sq = 219.33, reduction = 1.50
  09_03 P(X>=13) from table = 0.003, exact = 0.00369 (121/32768, slide 11)
  R6 exact scipy.stats.binom.sf(55, 400, 0.1) = 0.00664
```

All four PNGs were rewritten (mtime changed 16:15 -> 16:16).

**R6 value for T03: `binom.sf(55, 400, 0.1)` = 0.006637 (0.00664).** Independent check gives the same result: `0.006637435005285769`; `121/32768 = 0.003692626953125`.

## Results

| # | AC | Evidence | Result |
| :-- | :-- | :-- | :-- |
| 1 | runs in seconds, exit 0 | 1.4 s, exit 0 | PASS |
| 2 | writes 09_01, 09_02, 09_03 (+ optional 09_04) | all 4 present, fresh mtime | PASS |
| 3 | ruff clean | `uv run ruff check hw1/00_lecture_charts`: "All checks passed!" | PASS |
| 4 | 09_02 shows 220.83 / 219.33 | panel titles read `sum sq. distance = 220.83` / `219.33`; suptitle `reduction = 1.50 (0.7%)`; pooled mean 27.83. Values C={20,28,34}, D={22,26,37} match E3 and the shape of slide 9 `image22` | PASS |
| 5 | 09_03 overlays exact `1000*C(15,k)/2^15` | black points from `comb(15,k)*1000/2**15` sit on the bar tops; bar values match slide 11 `image28` row for row (0,0,3,14,42,92,153,196,196,153,92,42,14,3,0,0); k>=13 in warn color; vertical line at 13 | PASS |
| 6 | prints `binom.sf(55,400,0.1)` ≈ 0.00664 | printed 0.00664 | PASS |
| 7 | 121/32768 ≈ 0.00369 printed | printed `exact = 0.00369` | PASS |
| 8 | 09_04 (optional) | frequencies match slide 12 `image29` (7,40,93,173,327,253,73,27,7); person-count axis; lines at 54 (13.5%) and 56 (14%) show R7 | PASS |
| 9 | values live once with slide-source comments | slide data (puppy freq, alien freq, C/D values, A/B and E/F schematic values) each appear once with a slide comment. Minor: `400, 0.1` are repeated as literals in `_alien_null` (L158) and `build_all` (L178), and the threshold `13` appears in 4 places (L123, 128, 138, 139) with no named constant | PASS (minor note) |
| 10 | 09_01 matches slides 3-5, 7 | see ISSUE-1 | **ISSUE** |
| 11 | `main.py` diff append-only (one import + one build call) | see ISSUE-2 | **ISSUE (minor)** |

### ISSUE-1: 09_01 cites slide 4 but draws the opposite direction

- Evidence: in slide 3 `image11`, A ≈ 15 h and B ≈ 30 h, so A is faster. In slide 4 `image12`, A (green) sits at the top and B (red) at the bottom, so **A is slower**, and the slide says "35 hour difference". Slide 4 `image15` shows 9 repeats, and in every one A sits above B.
- Script (L28-29): `exp_a = [("Experiment 1\n(slide 3)", 45.0, 30.0), ("Experiment 2\n(slide 4)", 45.0, 10.0)]` with the comment "A always faster". The x-tick label "(slide 4)" is attached to a point where A is faster by 35 h, which contradicts the slide media it cites.
- Scope of the defect: this is a citation/fidelity mismatch. The panel's own lesson follows the §3.6 framing (H0 = no difference; a large, consistent gap leads to rejection), and R13 allows book-chosen schematic values, so that lesson is internally coherent. The defect is that the figure claims slide-4 provenance it does not have. T03's AC-W9 slide mapping will cite these slides.
- Classification: **implementation issue**, route to `developer`. Fix it in one of two ways: flip Experiment 2's direction to match `image12`, or drop the "(slide N)" tick labels and mark the panel as book schematic values.
- Notes (not route-backs): (a) architecture.md §3.6 "재실험도 같은 방향" does not say whether "same direction" is across repeats (true for `image15`) or relative to slide 3 (false). The architect may want to clarify it. (b) The suptitle cites "slides 3-5", but slide 5 (C/D, 13 h) is not drawn. That wording comes from the §3.6 table row itself, so it is a spec coverage gap, not a developer deviation.
- The right panel (E/F) matches slide 7: `image20` shows F above E by 0.25 h, and `image21` shows E above F by 0.5 h. PASS.

### ISSUE-2: main.py diff is not purely append-only

- `git diff --numstat` = `11 1`. The deleted line is the summary `print(f"wrote {len(deck5) + len(deck6)} figures ...")`. It was replaced by a version that also counts `deck9` and `skew`. The diff also registers `skew_cases` (import + build block), which belongs to the uncommitted 2.3.2 pass, not to T02. The working tree does not show which pass made the edit.
- The edit is functionally needed (the count would otherwise be wrong) and nothing was reformatted.
- Classification: **implementation issue, minor / non-blocking**. The owner or main session should either accept the single modified summary line or amend the AC. Commit the deck9 and skew_cases hunks under their own tasks.

## Verdict

**ISSUE.** Numeric ACs (4-8), run, ruff: PASS. The R6 value 0.00664 is confirmed and can go to T03. 09_01 (ISSUE-1) must be fixed before T03 cites `fig:ht-drug-experiments`. 09_02, 09_03 and 09_04 are usable as is.
