# T01r: verification of T01 (tab:analogy hypothesis-testing row)

- Reviewer: reviewer agent, cycle 1/2, 2026-09-23
- Target: `book_intro_2_ds/chapters/00-preface.tex`
- Spec: `research/PLAN.md` T01; `research/architecture.md` §5 (proposed row)
- ACs checked: AC-W1, AC-W3, AC-W4, AC-W12, "exactly one row", "no other line changes"; AC-W2 run as a bonus

## Results

| AC | Check | Result |
| :-- | :-- | :-- |
| one row | `git diff` shows one `+` line inside `tab:analogy` (line 26), after `분포 가정` row, before `\bottomrule` | PASS |
| wording | Concept `가설검정(Hypothesis Testing)`, analogy `\analogy{현장 측량으로 지도 검증}`, meaning cell are character-identical to architecture.md §5 proposed row | PASS |
| no other changes | `git diff --numstat` = `1 0`; single hunk | PASS |
| AC-W12 | deletions = 0 | PASS |
| AC-W3 | added lines `grep -cE '—\|---'` = 0 | PASS |
| AC-W4 | added lines `\ref/\autoref/\cref` = 0; hard-coded chapter numbers = 0 | PASS |
| AC-W1 | `latexmk -g -xelatex main.tex` (forced full rebuild) exit 0; 44 pages | PASS |
| AC-W2 | `grep -cE "undefined\|multiply defined" main.log` = 0 | PASS |

Bilingual (principle 1, not in T01's AC list): concept cell carries `(Hypothesis Testing)`, consistent with the other rows. `귀무가설` in the meaning cell has no English, but the existing rows' meaning cells carry none either and the text was prescribed verbatim by architecture.md §5.

## Notes (not ISSUEs)

- Q5 (owner approval for editing the preface table) is recorded in PLAN.md §1 L24 as a "default, owner may override" decision. The row is therefore in scope. The owner can still override it before commit.
- The build also compiled the unrelated uncommitted edit to `02-descriptive-statistics.tex`, and the build still exits 0.

## Verdict

**PASS.** No ISSUEs.
