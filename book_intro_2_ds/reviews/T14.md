# T14r review: deck4_basic_stats.py (cycle 1/2)

Reviewer, 2026-09-24. Target: `hw1/00_lecture_charts/deck4_basic_stats.py`, `figures/04_01..04_04`,
`hw1/00_lecture_charts/main.py` (deck 4 hunks only). Design: `research/arch-deck4.md` §4, §5, §6 + decision record.
AC: PLAN.md T14 says "as T02"; T02's original AC text was not found (PLAN.md L105 is only a closure line, no archive entry),
so the AC list below is the one given in the dispatch prompt (values once with slide comments, sklearn `load_iris` only with
no fitting, main.py append-only, ruff clean, type hints) + figure-vs-slide check.

**Verdict: ISSUE (implementation) -> `developer` cycle 2/2, one small fix bundling ISSUE-1 + ISSUE-2. All numbers PASS; T15 need not wait on the numbers, only on the 04_01 figure.**

## 1. Build

`uv run python hw1/00_lecture_charts/deck4_basic_stats.py`: exit 0, run once. All four PNGs written
(`04_01_dice_pmf_cdf.png` 44.8 KB, `04_02_triangle_pdf_cdf.png` 72.1 KB, `04_03_iris_cdfs.png` 66.0 KB,
`04_04_iris_mode_range.png` 33.9 KB, timestamps 14:48). No training, no `fit`; only `load_iris()` data access.

## 2. AC checklist

| AC | Check | Result |
| :-- | :-- | :-- |
| Figures exist | 04_01..04_04 present | PASS |
| sklearn load only | `grep fit\|train\|backward`: only docstrings, a comment and a legend label; `load_iris().data[:,0]` only | PASS |
| main.py append-only | `git diff`: +2 imports, +8 line block, 1 changed line (the `wrote {...}` total gains `len(deck3)+len(deck4)`). Same pattern as precedent commit 03888a0 (deck 7), which was accepted | PASS |
| ruff | `uv run ruff check deck4_basic_stats.py main.py`: All checks passed | PASS |
| Type hints | every function annotated (`-> None`, `-> str`, `-> list[str]`, `x: np.ndarray`) | PASS |
| `__all__` / module shape | `__all__ = ["build_all"]`, `build_all() -> list[str]`, `save()` from style | PASS |
| Values once + slide comment | slide 11 `FIFTEENTHS_*` and slide 9 `IRIS_BINOM_P/M` are single constants with `# source: slide N`. Dice PMF `1/6` appears 3 times (L31, L34, L98) with no constant; triangle density written twice (L125-128, L134) | ISSUE-2 |
| Slide 11 formula error (E1) | image17.png reads `P(X>k) = 1 - P(X<=x)`: k on the left, x on the right. Confirmed. Correct form `P(X>x) = 1 - F(x)`; numeric 0.6 still right. Script prints it with the note | PASS (design flag confirmed) |
| Variance convention (C1) | slide 9 `sigma^2 = 0.681` = ddof 0 (0.6811); ddof 1 = 0.6857 | PASS (design flag confirmed) |

## 3. Independent recompute (`uv run python -c`, Fraction / scipy.quad / Counter, separate from the script)

| Quantity | Independent | Script | Design | Result |
| :-- | :-- | :-- | :-- | :-- |
| Dice F(1..6) | k/6 | same | same | PASS |
| Dice E[X] | 7/2 | 3.5 | 3.5 | PASS |
| x/15 sum | 1 | 1 | 1 | PASS |
| F(3) | 2/5 = 0.4 | 0.4 | 0.4 | PASS |
| P(X>3) | 3/5 = 0.6 (also P(4)+P(5) = 0.6) | 0.6 | 0.6 | PASS |
| P(X>=3) | 0.8 | 0.8 | 0.8 | PASS |
| Triangle area (quad) | 1.0 | 1 | 1 | PASS |
| Triangle P(0.5<=X<=1.5) (quad) | 0.75 | 0.75 | 0.75 | PASS |
| Triangle E[X] (quad) | 1.0 | 1 | 1 (slide image23 1/3+2/3) | PASS |
| Range {1..5} -> {..,100} | 4 -> 99 | 4 -> 99 | 4 -> 99 | PASS |
| Mode {1,2,2,3,5} -> {..,100} | 2 -> 2 | 2 -> 2 | 2 -> 2 | PASS |
| Mode flip {1,2,2,3,3,3} -> {1,2,2,2,3,3} | 3 -> 2 | 3 -> 2 | 3 -> 2 | PASS |
| Iris n, count(x>=7) | 150, 13 | 13 | 13 | PASS |
| Iris mean | 5.8433 | 5.8433 | 5.8433 (slide 5.843) | PASS |
| Iris var ddof0 / ddof1 | 0.6811 / 0.6857 | same | same | PASS |
| Iris mode | 5.0 (10); runners-up 5.1, 6.3 (9 each) | 5.0, 10 | 5.0, 10 | PASS |
| Iris min/max/range | 4.3 / 7.9 / 3.6 | same | same | PASS |
| Binom F(0), p=0.087, m=10 | 0.40245 (0.913^10 = 0.402446) -> **0.4024** | 0.4024 | **0.4025** | NOTE-1 |
| Binom F(1) | 0.78594 -> **0.7859** | 0.7859 | **0.786(0)** | NOTE-1 |
| Binom F(2), F(3) | 0.9504, 0.9922 | same | same | PASS |

Side note: with p = 13/150 instead of the slide's rounded 0.087, F(0..3) = 0.4039, 0.7872, 0.9509, 0.9923. The script correctly uses the slide's 0.087 as §4.2 directs.

## 4. Figures vs slides (viewed)

- **04_01 dice PMF/CDF vs slide 10 text + slide 9 CDF style.** PMF: six bars at 1/6. CDF: steps at k/6, 0 below 1, 1 above 6. Title states F(3.5) = F(3) = 0.5. **Missing the open circles** at the left limit of each jump; design §6 asks for "열린/닫힌 점", and slide 9's binomial CDF (image9) draws them. Legend "F(k), closed at k" sits on the y=1 dotted line (cosmetic). ISSUE-1.
- **04_02 triangle vs slide 5/15 (image5, image22).** Same piecewise density; shaded [0.5, 1.5] labelled 0.75; CDF S-curve with F(1) = 0.5 marked. PASS.
- **04_03 iris CDFs vs slide 9 (image9).** Binomial steps at 0.40, 0.79, 0.95, 0.99, 1 match the slide panel; normal CDF with (5.843, 0.5) point matches the slide's (5.84, 0.5); empirical CDF overlay (optional per design). Binomial panel also has no open circles (slide has them; design does not require them here). Legend label "fitted normal CDF" is a moment match, not a fit; wording only. PASS.
- **04_04 iris mode/range vs slide 17 dot plot (image25).** Tallest column 5.0 with 10, matching the slide's dot plot; mean line 5.843; range arrow 4.3 to 7.9 labelled 3.6. The "range = 3.6" text is crossed by the mean dashed line (cosmetic). PASS.

## 5. ISSUEs

| ID | Class | Route | Finding | Fix hint |
| :-- | :-- | :-- | :-- | :-- |
| ISSUE-1 (blocking) | implementation | `developer` | 04_01 dice CDF lacks open circles at each jump (design §6 row 1 specifies "열린/닫힌 점"; slide 9 convention). Right-continuity is the point the figure teaches | add `scatter(k, cdf_left, facecolors="none")` at F(k-1); optional same for 04_03 binomial panel; move legend off y=1; relabel 04_03 "fitted normal CDF" to e.g. "normal CDF (sample mean, n-variance)" since nothing is fitted |
| ISSUE-2 (minor) | implementation | `developer` | "values once": dice PMF 1/6 repeated at L31, L34, L98 with no named constant or `# source: slide 10`; triangle density coded twice (L125-128, L134). `_confirm_exact_fractions` prints hand arithmetic for the triangle (`0.5 + 0.5`, `1/3 + 2/3`), so it re-prints rather than recomputes. Values are correct (independent quad above) | one `DICE_PMF` constant with slide-10 comment; one `_triangle_pdf(x)` helper used by both the plot and a numeric integral check |

NOTE-1 (no route): arch-deck4.md §4.2 binomial F(0) = 0.4025, F(1) = 0.7860 are half-up roundings of already-rounded 0.40245 / 0.78594; true 4-dp values 0.4024 / 0.7859. Resolved by the decision record (script value wins, both reported). T15 uses 0.4024 / 0.7859 (or 0.40 / 0.79), not §4.2.

No ladder/schema issue (book figure script, not system/app work; L1-L4 not applicable).
Deck 3 / deck 6 files and the deck3 hunks in main.py were not reviewed (another reviewer's scope).
